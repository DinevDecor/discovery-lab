# Knowledge Evolution Report KEV-0001

Demonstration report — not filed to a real Knowledge Base (see
`README.md`). Format per `../../ai-organization/employees/
AG-003-knowledge-curator/OUTPUTS.md`.

## Subject Knowledge Object(s)

"Recursive Adaptive Response" (not filed as a real `KO-NNNN` in this
walkthrough) — a candidate principle revised three times under its own
adversarial testing, per `RT-4`. Demonstrates the `supersedes`
relationship type (`RELATIONSHIP-ONTOLOGY.md`) end to end.

## Lifecycle timeline (`first_seen` → `last_seen`, each occurrence cited)

| Version | Date | State / label | Confidence | Relationship to prior version |
|---|---|---|---|---|
| v1 | `2026-07-06` | original "Recursive Adaptive Response" candidate | `0.93` | — (first appearance) |
| v2 | `2026-07-12` | "post-Breaker" revision | `0.55` | `supersedes` v1 |
| v3 | `2026-07-12` | "Injured Agent Condition" | `0.68` | `supersedes` v2 |
| v4 | `2026-07-12` | "Horizon & Domain Constraint" (`Falsifiability Repair`) | `UNKNOWN` | `supersedes` v3 |

`first_seen: 2026-07-06`, `last_seen: 2026-07-12`, `occurrences: 4`. v4's
numeric confidence is not stated in `RT-4`'s own text — recorded here as
`UNKNOWN`, not invented, per `../../ai-organization/employees/
AG-003-knowledge-curator/ROLE.md`'s escalation values.

## Confidence evolution

`0.93 → 0.55 → 0.68 → UNKNOWN` — a sharp drop at v2 (adversarial
"Breaker" testing, `RI-10`'s own defined mode, actually applied to this
claim), partial recovery at v3, and a scope-narrowing move at v4
(`Falsifiability Repair` — narrowing a claim's domain to make it
falsifiable again, rather than abandoning it). This is real, traceable
evidence that `RI-1`/`RI-2`'s Kernel judging protocol is not merely
aspirational — `RT-4`'s own stated significance in
`PILOT-RUN-0002-recovery-report.md`.

## Maturity trajectory

Single source, single AG-002 run throughout — `maturity` for this chain
would be `Recurring` at best under `LIFECYCLE.md`'s definition (four
occurrences, one source), the same ceiling `KO-0001` sits at, for the
same underlying reason (see `KO-0001`'s own maturity note). This is
recorded descriptively; this report proposes no promotion by itself
(`OUTPUTS.md`'s "Descriptive only" rule for Knowledge Evolution Reports).

## Provenance

`../../ai-organization/employees/AG-002-discovery-archaeologist/runs/
PILOT-RUN-0002-recovery-report.md`, `RT-4`.
