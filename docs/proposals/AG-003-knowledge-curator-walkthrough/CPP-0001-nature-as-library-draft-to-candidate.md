# Core Principle Proposal CPP-0001

Demonstration proposal — not filed to a real Knowledge Base (see
`README.md`). Format per `../../ai-organization/employees/
AG-003-knowledge-curator/OUTPUTS.md`.

## Subject Knowledge Object

`KO-0001` — "Nature as a library of architectures"
(`KO-0001-nature-as-library-of-architectures.md`).

## Current status

`Draft`.

## Proposed status

`Candidate Principle` — **exactly one step**, per `../../ai-organization/
employees/AG-003-knowledge-curator/PROMOTION-RULES.md`'s "one step at a
time" rule. This proposal does not, and could not yet, propose
`Validated Principle`.

## Evidence against `PROMOTION-RULES.md`'s threshold for this step

`Draft → Candidate Principle` requires: `occurrences >= 2` from distinct
citations; `maturity >= Recurring`; no open Contradiction Report against
the object.

- `occurrences: 5` (`KO-0001`'s own `provenance`) — five distinct diary
  dates (`2026-06-25`, `06-26`, `06-27`, `07-10`, `07-11`), each a
  separate citation AG-002 itself recorded, not a single quotation
  double-counted.
- `maturity: Recurring` (`KO-0001`'s own computed value) — meets the
  minimum for this step.
- No Contradiction Report exists against `KO-0001` as of this
  walkthrough (see `CONTRADICTION-CHECK-0001.md` — the one tension
  screened in this walkthrough concerns a different Knowledge Object).

All three conditions are met. This is, on AG-002's own evidentiary
framing, *"the strongest signal in the whole archive of what its author
considered most important"* (`RT-3`'s own closing line) — a strong case
for `Candidate Principle`.

## What this proposal does NOT claim

- It does **not** claim `Validated Principle` is warranted.
  `PROMOTION-RULES.md` requires `maturity: Convergent` for that step —
  at least two *independent* sources or AG-002 runs. All five citations
  here trace to the same diary, read in a single run
  (`PILOT-RUN-0002`) — see `KO-0001`'s own "Why `maturity: Recurring`,
  not `Convergent`" note. Five restatements within one source is real,
  strong internal recurrence; it is not yet the cross-source convergence
  the next step requires.
- It does not assert the idea is true, correct, or scientifically valid
  — only that it is well-evidenced as a recurring, author-emphasized
  concept within the recovered material, which is what `Candidate
  Principle` status represents in this Role's own lifecycle
  (`LIFECYCLE.md`), not a claim about the external world.
- It does not modify `KO-0001.status` — filing this proposal changes
  nothing by itself; only a human decision (or an accepted Knowledge
  Review recommendation) would.

## Recommendation

Accept `Draft → Candidate Principle`. Do not consider
`Candidate → Validated` until a second independent source or a second
AG-002 run corroborates the idea.

## Provenance

`../../ai-organization/employees/AG-002-discovery-archaeologist/runs/
PILOT-RUN-0002-recovery-report.md`, `RI-5`, `RT-3`.
