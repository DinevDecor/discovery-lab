# Deliverable 2 — Principle Survival Table

Per `META-001` Phase 5. Ratings: `Unsupported` (no independent evidence)
/ `Weak` (1 of 4 independent domains) / `Moderate` (2 of 4) / `Strong`
(4 of 4, but not fully cleared of Phase 4's falsification objections) /
`Cross-domain Stable` (4 of 4, and specifically shown to survive the
falsification attempt in `5-COUNTER-THEORY.md`). Domain count uses the
**effective independent domain count of four** (`kod`, `trust-engine`,
`project-memory`, `discovery-lab`) — see `1-CROSS-DOMAIN-EVIDENCE-
MATRIX.md`'s independence disclosure; Groups A+B and D+E are each one
domain, not two.

## `RI-0002`'s own candidate principles

| Principle | Domains | Rating | Evidence |
|---|---|---|---|
| **P3 — Named uncertainty states, never silently resolved** (the sharpest form of `RI-0002` Principle 1) | 4 / 4 | **Cross-domain Stable** | `kod`: `BLOCKED`+criterion, Guardian's 6 fixed outputs. `trust-engine`: `UNKNOWN`, `INSUFFICIENT_EVIDENCE` with explicit misuse rules. `project-memory`: `unresolved` list always shown, `Architecture–Implementation Drift`. `discovery-lab`: 4-category Organizational Principle. Four structurally distinct vocabularies, not a shared template — the only principle `5-COUNTER-THEORY.md`'s falsification attempt could not explain away. |
| **P1 — AI proposes, human commits/approves** (`RI-0002` Principle 1, human/AI axis) | 4 / 4 | **Strong** | Present in all four domains with real mechanism, not just wording — but `5-COUNTER-THEORY.md` raises a live, unresolved concern that this specific pattern may be a generic 2024–2026 AI-tooling convention rather than a domain-independent discovery, so it is held one tier below `Cross-domain Stable`. |
| **P2 — One authoritative representation; disagreement never auto-resolved** (`RI-0002` Principle 1, state-management axis) | 4 / 4 | **Strong** | `kod`'s Authority Matrix, `trust-engine`'s Trust Memory vs. raw evidence, `project-memory`'s two-plane split, `discovery-lab`'s Knowledge Base concept — real architectural convergence, same caveat as `P1` about generic-pattern risk. |
| **P4 — Role separation (validator ≠ generator)** (`RI-0002` Principle 1, structural precondition) | 4 / 4 | **Strong** | `kod`'s Writer Matrix and 3-engine split, `trust-engine`'s 5-step review with separate responsibilities, `project-memory`'s `INV-5` (explicit, named), `discovery-lab`'s 4 kept-distinct senses of "review." |
| **P7 — Process over conclusion, stated as an explicit principle** (`RI-0002` Principle 1, philosophical framing) | 3 / 4 | **Moderate** | Strongly named in `kod` and `trust-engine` (the latter nearly verbatim: "reality is the final arbiter"); present but reframed as an evidentiary rule, not an epistemic one, in `project-memory`; structurally enacted but not named as its own principle in `discovery-lab`. Real, but not universal in the same way `P3` is. |
| **Generative abstraction** (`RI-0002` Principle 2 — strip domain-specific detail across systems to find an invariant process) | 0 / 4 | **Unsupported** | No source group states, implies, or instantiates this method. A clean, confirmed absence — not softened to protect the theory. |

## New principles discovered (not proposed by `RI-0002`)

See `3-NEW-PRINCIPLES-DISCOVERED.md` for full detail; rated here for
completeness against the same five-level scale.

| Principle | Domains | Rating |
|---|---|---|
| Named artifact/task ownership (exactly one writer/owner at any moment) | 2 / 4 (`kod`, `project-memory`) | Moderate |
| A numeric score gates an escalation *tier*, never the final action | 2 / 4 (`trust-engine`, `project-memory`) | Moderate |
| Two-layer authority model with a named mismatch state (`Drift`) | 1 / 4 clearly (`project-memory`), partial echo in `kod` | Weak |
| "Anti-theater" self-check (repeated `PASS` is a red flag, not success) | 1 / 4 (`project-memory`) | Weak |

## What this table means for `META-001`'s question

Of `RI-0002`'s two stated principles, one (validation discipline, best
represented by `P3`) is the best-supported finding in this entire
report — `Cross-domain Stable`, the highest rating available, held by
only one principle. The other (generative abstraction) is
`Unsupported` outside the diary. This is not a uniform result in either
direction, and the table is not adjusted to make it look more uniform
than it is. See `6-FINAL-VERDICT.md`.
