# manifests/

One record per file ever dropped into `../📥 DROP HERE/`, created and
maintained by the agent/steward — not something a human writes by hand.
Schema and lifecycle: `../PROCESSING-PROTOCOL.md`.

Not a folder a human ever needs to open as part of the normal workflow —
`../INDEX.md` is the human-readable summary.
