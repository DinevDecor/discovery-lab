# Claim Ledger — 7-Day Manual Validation (CLV-001)

**Status:** IN PROGRESS
**Started (setup):** 2026-08-16 13:54 UTC
**Day 1 begins:** with the first Business Candidate Analyst output produced *after* this setup — the 22:00 UTC 2026-08-16 run of `constraint-archaeology-daily.yml` (Constraint Archaeology → Business Candidate Analyst), or its report file, whichever lands first.
**Ends:** the experiment runs for **at least** 7 consecutive calendar days from Day 1's first logged claim, **and** requires **at least** 20 decision-relevant claims — both conditions must hold, not either. Concretely: if 20 claims accumulate before day 7, logging/reconciliation continues anyway through the end of day 7, and the experiment concludes then. If day 7 arrives with fewer than 20 claims logged, the experiment continues past day 7, logging only, until 20 decision-relevant claims are reached. The moment both the 7-day minimum and the 20-claim minimum are satisfied, the experiment stops immediately and the final report is issued. (Corrected 2026-08-16, before Day 1 began — the original scaffold below used an "OR, whichever first" rule; nothing else about the schema, save criteria, or daily process changed.)
**This is not a software task.** No product, service, database, or BCA logic change is created by this file or this process. This is a manual, append-only research log.

---

## 0. What this validates

One question only:

> Does an explicit claim ledger prevent materially wrong analytical memory, claim merging, promotion, or reuse often enough to justify becoming a standalone tool?

This document is the append-only ledger itself. Nothing above this line, and nothing in an already-appended entry below, is edited once written. A correction is a new entry that references the claim ID it corrects.

Adversarial framing actively pursued during logging, per instruction: **prefer evidence that "Claim Ledger is merely good research hygiene, not a standalone product"** over evidence that manufactures a save.

---

## 1. What counts as a logged claim

Only a claim expressible as one falsifiable sentence that could materially affect: ADVANCE/REJECT/WATCH/CHEAP_TEST state, prioritization, capital allocation, research direction, interpretation of market structure, assessment of competition, or belief that a constraint/opportunity actually exists. Not logged: scene-setting, restated context, prose quality, or anything not decision-relevant. Real BCA candidates/results only — no claim here is backfilled from data older than this experiment's start.

## 2. Claim record schema

Each entry uses this exact field set:

```
### CLV-NNN
- Timestamp / as-of:
- Candidate / workflow:
- Exact claim:
- Claim type: OBSERVATION | EVIDENCED_FACT | INFERENCE | ASSUMPTION | INSUFFICIENT_DATA
- Evidence tier: T0 (no external evidence) | T1 (single secondary/repeated source) | T2 (multiple independent secondary sources) | T3 (primary source) | T4 (independently measured / directly observed)
- Sources:
- What would falsify it:
- Decision dependency:
- Reused from earlier claim?: NO | CLV-xxx
- Same claim or changed claim?: SAME | NARROWER | BROADER | CONTRADICTS | IDENTITY_BREAK | N/A (first mention)
- Current status: OPEN | SUPPORTED | WEAKENED | FALSIFIED | SUPERSEDED | UNRESOLVED
```

## 3. Critical event log (CLAIM_LEDGER_SAVE)

A `CLAIM_LEDGER_SAVE` is counted **only** when the ledger exposes something that likely would have been missed without it (a claim reused with quietly-weaker evidence than remembered, an inference later misremembered as observed fact, two distinct claims silently merged, a falsified claim still influencing a later decision, a "second source" that's actually the same upstream source, stale evidence on a still-cited claim, `INSUFFICIENT_DATA` misremembered as a negative conclusion, or a decision reversed once the original claim/evidence was reconstructed). Formatting, easier note-taking, generic traceability, or "nice to have the history" do **not** count. Logged below as they're found, not retroactively inflated at report time.

**SAVE-001** (2026-08-18, Day 3, from CLV-002 reconciliation): The anomaly ID `ANOM-0362` was confirmed, on two independent checks two days apart, to have pointed at three different underlying observation sets (Day 0/probe: fly.io idle-machines, 1 obs; Day 1: orphaned-subagents, unrelated content; Day 3: fly.io idle-machines again, 2 obs) — and the same pattern independently reproduced on `BC-0130`'s own `anomaly_ids` field (`[ANOM-0321, ANOM-0392]` → `[ANOM-0324, ANOM-0396, ANOM-0432]` across the same window, same candidate, same core evidence). This is a "stale evidence citation on a still-cited claim" pattern — a reference that reads as durable but silently isn't — and it would very likely have been missed without a persistent record spanning the two check dates: reading either day's `anomalies.json` alone, in isolation, gives no indication the ID is unstable; only comparing today's content against a fixed record of what the ID held two days ago exposes it. **Caveat kept attached, per the adversarial framing this experiment asked for:** this does not appear to affect BCA's own promotion arithmetic, which keys off `OBS-####` IDs and source names (both stable across the same runs), not `ANOM-####`. The practical risk is narrower than "this breaks the pipeline" — it is "any *report* (including this repository's own research artifacts) that cites an `ANOM-####` as if it were a durable reference is citing something less stable than it appears."

**SAVE-002** (2026-08-18, Day 3, from CLV-001 reconciliation): `BC-0130`'s VALIDATING promotion rests on grouping what read, on manual reciprocal-repair analysis against this repository's own stated same-mechanism standard, as three distinct failure mechanisms (pre-execution spend-ceiling gap; idle-machine monitoring gap; pricing-model confusion) under one candidate, inflating its apparent "2 distinct sources" into what may functionally be three separate single-source opportunities miscounted as one multi-source one. A reader trusting the report's `VALIDATING` label at face value — the only signal a non-ledger read would surface — would not see this; it only surfaces by explicitly restating the claim as falsifiable ("these anomalies are the same underlying opportunity") and checking it against a named standard, which is exactly the discipline the ledger schema forces and a quick read does not.

## 4. Daily run log

| Day | Date (UTC) | BCA report checked | New claims logged | Reconciliation done | Notes |
|---|---|---|---|---|---|
| 0 (setup) | 2026-08-16 | `business-candidates-2026-08-15.md` (baseline only, not logged from) | 0 | — | Ledger created; waiting for first new run |
| 1 | 2026-08-16 | `business-candidates-2026-08-16.md` (real scheduled run 31... completed 22:57:58Z, merged into this branch) | 2 (CLV-001, CLV-002) | N/A — no prior OPEN claims to reconcile yet | One real lifecycle escalation (WATCH→VALIDATING) landed on Day 1; both logged claims are about that escalation's evidentiary basis, not manufactured for volume |
| 2 | 2026-08-17 | none — no new report produced | 0 | None possible — no new data; CLV-001/CLV-002 remain OPEN unchanged | Real pipeline failure, not a delay: run `32075974441` (schedule, main), unit-test steps 4–6 all passed, step 7 "Run daily pipeline" itself failed after 26 min (22:26:04Z→22:52:52Z), commit step correctly skipped. `main` still at yesterday's head (`f2ff868`). Per protocol: 0 claims logged, no diagnosis/fix attempted (out of scope for this experiment) |
| 3 | 2026-08-18 | `business-candidates-2026-08-18.md` (pipeline recovered; `material-events-2026-08-18.json` empty — no lifecycle transitions today) | 0 new; 2 reconciled (CLV-001, CLV-002) | Both CLV-001 and CLV-002 reconciled against fresh data; **2 CLAIM_LEDGER_SAVE events logged (SAVE-001, SAVE-002)** | No new claims manufactured for volume — today's work was entirely reconciliation of Day 1's open claims, which is where the material findings actually landed |
| 4 | 2026-08-19 | `business-candidates-2026-08-19.md` (real material event: BC-0039 WATCH→VALIDATING) | 1 new (CLV-003); 2 reconciled (CLV-001, CLV-002, no status change) | CLV-001/CLV-002 stable, no new saves; CLV-003 logged (same false-merge pattern as CLV-001, second independent instance) | CLV-003's underlying candidate (BC-0039) is its own new material event, not a reconciliation — logged per the same bar as Day 1, not for volume |
| 5 | 2026-08-20 | `business-candidates-2026-08-20.md` (real material event: BC-0117 WATCH→VALIDATING) | 1 new (CLV-004, resolved FALSIFIED same-day); 2 reconciled (CLV-001, CLV-002, no status change) | **1 new CLAIM_LEDGER_SAVE (SAVE-003)** — clearest instance yet, directly falsified same-day by reading the two source anomalies' own text; `ANOM-0362` independently hit its 4th unrelated content-swap, corroborating SAVE-001 further (not double-counted) | BC-0117 was initially expected to be a counter-example (well-merged); checking the raw source anomalies overturned that on the same day — logged honestly per the adversarial instruction |
| 6 | 2026-08-21 | none — no new report produced | 0 | None possible — no new data; CLV-001–CLV-004 remain unchanged | Real pipeline failure, second one this experiment (first was Day 2): run `32532987506` (schedule, main, head_sha `9dc4e8c`), unit-test steps 4–6 passed, step 7 "Run daily pipeline" failed after ~21 min (22:26:56Z→22:48:04Z), commit step correctly skipped. `main`'s only new commits tonight were unrelated bots (translation refresh, tool-radar signals) — not pipeline output. Per protocol: 0 claims logged, no diagnosis/fix attempted |
| 7 | 2026-08-22 | `business-candidates-2026-08-22.md` (report timestamp 11:00 UTC, not the usual ~22:00 — main shows a "Trigger CA/BCA recovery run" commit and a "Harden daily CA/BCA scheduling and persist failure diagnostics" commit, i.e. a human/other session manually recovered and hardened the pipeline after Days 2 & 6's failures) | 0 new; 1 reconciled (CLV-003) with a major status change; CLV-001/CLV-002 also checked, unchanged | **1 new CLAIM_LEDGER_SAVE (SAVE-004)** — the most consequential of the experiment: `BC-0039` reached the pipeline's first-ever `PROMISING` today; tracing CLV-003 (opened Day 4) through to this event shows the promotion bundles 3 unrelated domains/buyer personas. `ANOM-0362` independently swung to a 5th distinct value today, further corroborating SAVE-001, not double-counted | Day 7 (the 7-day minimum) is reached today, but only 4 decision-relevant claims are logged — the 20-claim minimum is not met, so per the corrected AND stop condition this experiment continues past today, logging only, until 20 claims accumulate |
| 8 | 2026-08-23 | none new on `main` (still `bf28d86`, unchanged from Day 7 — no `business-candidates-2026-08-23.md` exists on `main`) | 0 | None needed — `main`'s BC-0130/ANOM-0362/BC-0039 state is byte-identical to Day 7, already reconciled | **Process anomaly, noted not chased:** `origin/claude/session-archaeology-review-v3184e` (this experiment's own branch) received a commit (`f86c846`, `constraint-archaeology-bot`, 2026-08-22T23:56:35Z) re-running the daily pipeline against **this branch instead of main** — real bot output, same commit-message format as every other day, but pushed to the wrong ref, diverging from `main`'s own trail for the same calendar date. It does not touch this ledger file. Per the same discipline applied to the two pipeline failures: `main` remains the sole source treated as "real daily BCA output" for this experiment; this stray commit is recorded as a fact, not treated as a new day's data, and not investigated or fixed (out of scope) |
| 9 | 2026-08-24 | `business-candidates-2026-08-23.md` (main caught up; report timestamp 2026-08-23T23:27:39Z; `material-events-2026-08-23.json` empty — no lifecycle transitions) | 0 new; 3 reconciled (CLV-001, CLV-002, CLV-003/already-FALSIFIED — no status change on any) | None new — `BC-0130` and `BC-0039` both unchanged from Day 7/8; `ANOM-0362` swung to a **6th** distinct unrelated value (UAE shareholder-resolution generation), further saturating SAVE-001, not double-counted | **Merging today required resolving a real conflict** — Day 8's stray branch commit (`f86c846`) and `main`'s own subsequent commit both modified the same generated data files independently, producing genuine merge conflicts in 5 files. Resolved by taking `main`'s version entirely (`git checkout --theirs`) for every conflicted generated/data file, discarding the stray commit's content, consistent with treating `main` as sole source of truth. Verified byte-identical to `main` post-resolution before reconciling |
| 10 | 2026-08-25 | none new on `main` (latest BCA-touching commit still `e47ebf1` from Day 9) | 0 | None needed — no new data | **Recurring reliability pattern, noted not chased:** GitHub Actions run history shows two more real failures since Day 9 (run #35, `head_sha=05a2d49`, failed 22:47→23:11 UTC on 08-24; run #36, `head_sha=2b4c0e8`, failed 00:01→01:05 UTC — this one got as far as writing new `observations.jsonl` + `pipeline-status` before failing, never reaching the BCA report step) — bringing the total to at least 4 real failures across 10 days (Days 2, 6, and these two). A further run (#37) was **in progress** at the moment of this check-in (started 22:48 UTC, ~25 min before this check), not yet resolved either way. Per established discipline: 0 claims logged, failure pattern recorded as fact, not diagnosed or fixed (out of scope) |
| 11 | 2026-08-26 | `business-candidates-2026-08-25.md` (report timestamp 2026-08-25T23:58:12Z; clean merge, no conflicts; `material-events-2026-08-25.json` empty — no lifecycle transitions) | 0 new; 3 reconciled (CLV-001, CLV-002, CLV-003 — no status change on any) | None new — `BC-0130` gained a 4th observation (evidence accretion only, no state change, still 3 unrelated threads per CLV-001); `BC-0039` unchanged, still `PROMISING`/FALSIFIED; `ANOM-0362` swung to a **7th** distinct unrelated value (VPN hub-and-spoke topology/encryption overhead), further saturating SAVE-001, not double-counted | Yesterday's in-progress run (#37) appears to have succeeded — `main` produced this report — though a `pipeline-status-2026-08-26.json` also exists with no matching `business-candidates-2026-08-26.md` yet, suggesting last night's (08-26) run may be partially through or not yet complete at check-in time; not chased further |
| 12 | 2026-08-27 | none new on `main` (latest BCA report still 08-25) | 0 | None needed — no new data | **Reliability pattern worsening, still not chased:** 3 more real failures since Day 11 (runs #38 `head_sha=b0246e0`, #39 `head_sha=0a20ad9`, #40 `head_sha=9b914bd`), none reaching the BCA report step — two of them got only as far as appending to `observations.jsonl` before failing. Since Day 7's recovery, essentially every scheduled run except the one that produced Day 11's data has failed. Running tally across the experiment: at least 7 real pipeline failures in 12 days. Recorded as fact for the final report's failure-pattern section; still not diagnosed or fixed (out of scope) |
| 13 | 2026-08-28 | none new on `main` (latest BCA report still 08-25) | 0 | None needed — no new data | 2 more real failures today (runs #41, #42, both `head_sha=c5c9035`), both before reaching the BCA report step. Running tally: at least 9 real pipeline failures across 13 days — `main` has produced usable new BCA output on only 6 of 13 days |
| 14 | 2026-08-29 | `business-candidates-2026-08-29.md` (report timestamp 05:16:37Z; large backlog catch-up — 157 new observations, clean merge; `material-events-2026-08-29.json` empty — no lifecycle transitions) | 0 new; 3 reconciled (CLV-001, CLV-002, CLV-003 — no status change on any) | None new — `BC-0130` still VALIDATING (more accumulated evidence, same 3-thread pattern), `BC-0039` still PROMISING/FALSIFIED, `ANOM-0362` unchanged from Day 11's value (VPN topology) — first repeat value observed, though several failed days sit between the two checks so continuity can't be confirmed | Pipeline recovered again after Days 12–13's run of failures; this run's timestamp (05:16 UTC) suggests it ran well outside the usual ~22:00 schedule, consistent with a backlog catch-up after the failure streak |
| 15 | 2026-08-30 | none new on `main` (latest BCA report still 08-29) | 0 | None needed — no new data | 2 more partial-progress commits today (`d3a3fd7`, `990e104` — same content), both only appending `observations.jsonl` + `pipeline-status`, never reaching the BCA report step. Same recurring failure signature as prior days |
| 16 | 2026-08-31 | none new on `main` (latest BCA report still 08-29) | 0 | None needed — no new data | 2 more partial-progress commits today (`f260295`, `5f3cd7c`), same signature — `observations.jsonl` only, never reaching the BCA report step |
| 17 | 2026-09-01 | none new on `main` (latest BCA report still 08-29) | 0 | None needed — no new data | 1 more partial-progress commit today (`a225358`), same signature — `observations.jsonl` + `pipeline-status` only, never reaching the BCA report step. `main` has now gone 3 consecutive calendar days (08-30, 08-31, 09-01) without producing new BCA output |
| 18 | 2026-09-02 | none new on `main` (latest BCA report still 08-29) | 0 | None needed — no new data | 1 more partial-progress commit today (`2084a07`), same signature. `main` has now gone 4 consecutive calendar days (08-30 through 09-02) without producing new BCA output |
| 19 | 2026-09-03 | `business-candidates-2026-09-03.md` (report timestamp 02:09:42Z; large 4-day backlog catch-up, 252 new observations, clean merge; `material-events-2026-09-03.json` empty — no lifecycle transitions) | 0 new; 3 reconciled (CLV-001, CLV-002, CLV-003 — no status change on any) | None new — `BC-0130` still VALIDATING; `BC-0039` gained a **4th** unrelated thread today ("development server process memory allocation"), further corroborating its already-FALSIFIED status, not a new save; `ANOM-0362` swung to an **8th** distinct unrelated value (serverless sprite wake/restart failure), further saturating SAVE-001 | Pipeline recovered after the 4-day failure streak (Days 15–18) |
| 20 | 2026-09-04 | none new on `main` (latest BCA report still 09-03) | 0 | None needed — no new data | 2 more partial-progress commits today (`486ce34`, `cdfd63c`), same recurring signature — `observations.jsonl` only, never reaching the BCA report step |

## 5. Claims

### CLV-001
- Timestamp / as-of: 2026-08-16T22:59:16Z (BCA report timestamp)
- Candidate / workflow: BC-0130 (Business Candidate Analyst, Mode A) — lifecycle escalation WATCH → VALIDATING, recorded in `business-candidate-analyst/reports/material-events-2026-08-16.json` as `state_changed:BC-0130`, reason "reached VALIDATING: distinct_sources>=2 (have 2); economic_consequence EVIDENCED"
- Exact claim: BC-0130's two grouped anomalies — ANOM-0321 ("Enforcing spend ceiling per user session or agent run when LLM calls paid external functions") and ANOM-0392 ("Tracking cloud infrastructure cost and machine provisioning state — idle resources ran unnoticed... accumulating $600 charge") — describe the same underlying business opportunity.
- Claim type: INFERENCE
- Evidence tier: T1 (single source: BCA's own grouping output; not independently corroborated that these are one mechanism)
- Sources: `business-candidate-analyst/data/candidates.json` (BC-0130 record, `anomaly_ids: [ANOM-0321, ANOM-0392]`); `constraint-archaeology-agents/data/anomalies.json` (ANOM-0321, ANOM-0392 canonical_pattern text); `business-candidate-analyst/README.md` §"Deduplication: same underlying opportunity, not same wording" (merge rule: `buyer_bucket` + `function_class` match + ≥2 shared taxonomy keywords vs. a single anchor observation — explicitly a keyword/bucket heuristic, not `ca_agents.same_mechanism_gate`'s reciprocal-repair test; the same README states plainly that "false merges within a bucket are structurally possible even after these fixes")
- What would falsify it: Running (or manually reasoning through) a reciprocal-repair test on ANOM-0321 vs. ANOM-0392 — does fixing "add a pre-execution spend ceiling to metered LLM function calls" also fix "no alerting when a manually-scaled-up machine is never scaled back down," and vice versa? If the fixes are independent (plausible: one is a pre-execution budget/approval gate, the other is a post-hoc resource-lifecycle/idle-detection monitor — the exact same two-layer distinction drawn in `docs/research/ai-agent-cost-governance-opportunity-probe.md` §2's Layer C vs. Layer D split), the claim is falsified.
- Decision dependency: If falsified, BC-0130's VALIDATING state is a false positive from this run's own stated promotion rule ("distinct_sources>=2") — the "2 sources" are actually 2 different opportunities each still resting on 1 source, and correct treatment is two separate WATCH candidates (frequency still INSUFFICIENT_DATA each), not one VALIDATING candidate. Matters directly to research prioritization: VALIDATING is meant to signal stronger multi-source corroboration than either sub-claim independently has.
- Reused from earlier claim?: NO (first mention in this ledger; thematically adjacent to, but not reused from, the "E. Agent Cost Governance" finalist scored `recurrence: 3/10` in the prior Claim Ledger opportunity-archaeology artifact — noted as a cross-reference only, not counted as reuse)
- Same claim or changed claim?: N/A — first mention
- Current status: OPEN

**Reconciliation note, 2026-08-18 (Day 3):** BC-0130 gained a third observation today (`OBS-20260818-0022-9ac98d`), still within the same 2 distinct sources (fly-io, openai-devs) — `independent_observation_count` went 2→3, `evidence_diversity` unchanged. The new observation's text is explicitly about **pricing-model confusion** ("customer (non-technical) trying to understand pay-as-you-go vs fixed pricing... confusion about pricing structure; user expects fixed monthly plans but provider uses metered billing") — a *third*, textually distinct thread now folded into the same candidate alongside the original two (pre-execution spend-ceiling gap; idle-machine monitoring gap).
Applying this repository's own same-mechanism standard (`CLAUDE.md`: "Two anomalies merge only if each one's own repair removes the other's failure, in both directions") as a manual reciprocal-repair check across all three threads: a pre-execution budget gate on metered calls does not fix idle unmonitored machines; idle-machine alerting does not fix a model over-calling a metered function mid-run; and clearer pricing documentation fixes neither. None of the three repairs the other two. By this repository's own documented standard, **these read as three different mechanisms, not one** — this is my own reasoned inference applying that standard, not a pipeline-verified fact (BCA does not run the reciprocal-repair test itself; its own README states its merge rule is a coarser keyword/bucket heuristic).
Classification: **CONTRADICTS** (the claim that these are "the same underlying opportunity" is contradicted by this repository's own same-mechanism standard, applied manually). Current status updated: **WEAKENED** (not FALSIFIED outright — I have not run the actual `ca_agents.same_mechanism_gate` code against this text, only reasoned by hand from its stated principle; a stronger falsification would run the real gate).

### CLV-002
- Timestamp / as-of: 2026-08-16T22:59:16Z (anomalies.json as merged into this branch) vs. 2026-08-16T07:38Z (probe's own stated timestamp, same calendar day, ~15h earlier)
- Candidate / workflow: `constraint-archaeology-agents` anomaly registry, cross-referenced against `docs/research/ai-agent-cost-governance-opportunity-probe.md` §1's evidence table (a same-repository artifact produced earlier the same day, before this experiment began)
- Exact claim: The anomaly ID `ANOM-0362`, as it currently reads in `constraint-archaeology-agents/data/anomalies.json` after today's run, has a different canonical_pattern (orphaned subagents, `OBS-20260815-0031-8266d1`) than the pattern the same-day-earlier opportunity probe attributed to `ANOM-0362` (fly.io idle machines / ~$600, `OBS-20260815-0022-5480ed`) — that observation now appears un-anomaly-numbered the same way in this run's file (it is `BC-0139`'s cited evidence, but its current `ANOM-####` grouping was not independently re-confirmed this pass).
- Claim type: OBSERVATION
- Evidence tier: T4 (directly diffed two committed artifacts — the probe's own table text vs. today's `anomalies.json` content for the same ID)
- Sources: `docs/research/ai-agent-cost-governance-opportunity-probe.md` §1 table (branch `claude/ai-agent-cost-governance-probe-9axch5`, commit `8a2f922`); `constraint-archaeology-agents/data/anomalies.json` as of commit `f2ff868` (merged into this branch today)
- What would falsify it: Re-reading the probe's table and finding a transcription error on my part (i.e. it never actually said `ANOM-0362` = fly.io); or discovering `anomalies.json`'s ID space is sharded/scoped in a way that makes two same-numbered entries not actually comparable (e.g. per-run files rather than one running file) rather than one flat, reused ID space.
- Decision dependency: Any report — including this repository's own prior research artifacts — that cites a constraint-archaeology `ANOM-####` as if it were a stable, durable reference risks silently pointing at different content a day later. This repo's own `CLAUDE.md` already documents `anomalies.json` as a **snapshot file, not an append-only ledger** ("Snapshot files remain runtime truth... Do not migrate readers") — so the *underlying* non-durability is expected/known infrastructure behavior, not a hidden defect. The open question for reconciliation is narrower: whether a persistent claim ledger is what actually catches the risk of a *report* citing a snapshot ID as if it were durable — or whether that risk is equally visible without one, in which case this is closer to a false positive than a save. Marked OPEN, not pre-judged.
- Reused from earlier claim?: NO
- Same claim or changed claim?: N/A — first mention
- Current status: OPEN

**Reconciliation note, 2026-08-18 (Day 3):** checked `ANOM-0362` a third time. Today it reads: canonical_pattern "Managing cloud compute machine lifecycle after parallel batch jobs complete — 40 idle machines ran unnoticed for two weeks... $600", `observation_ids: [OBS-20260815-0022-5480ed, OBS-20260818-0077-df0218]`, `first_seen: 2026-08-18`. This is the fly.io story again — matching the *original probe's* content (Day 0) better than Day 1's content did — but it is not identical to either prior state: Day 0 (probe) cited exactly one observation (`...5480ed`); Day 1 cited zero fly.io observations (orphaned-subagents instead); Day 3 cites two (`...5480ed` plus a new `...df0218`). `first_seen` also reset to `2026-08-18`, even though `...5480ed` was first captured 2026-08-15 — confirming this field tracks the anomaly *record's* lifetime, not the underlying observation's age.
Independently, `BC-0130`'s own `anomaly_ids` field (checked directly in `candidates.json`) also changed this run: `[ANOM-0321, ANOM-0392]` (Day 1) → `[ANOM-0324, ANOM-0396, ANOM-0432]` (Day 3) — three different numbers, for the same stable `candidate_id` and (mostly) the same underlying `OBS-####` evidence. Across both checks, the pattern is now consistent and reproducible: **`ANOM-####` numbering is rebuilt/reassigned on every pipeline run; it is not a persistent identifier**, while `BC-####` (candidate) and `OBS-####` (observation) evidence IDs are stable across the same runs.
Classification: **IDENTITY_BREAK**, now confirmed on two independent checks (Day 1 and Day 3), not merely flagged. Current status updated: **SUPPORTED** (the claim as stated — that the ID drifted and doesn't durably identify content — holds up; the caveat noted on Day 1, that this doesn't touch BCA's own promotion math since that keys off `OBS-####`/source names rather than `ANOM-####`, also still holds and is worth keeping attached to this finding so it isn't overstated).

**Reconciliation note, 2026-08-19 (Day 4):** checked both again. `ANOM-0362` unchanged in content from Day 3 (same canonical_pattern, same 2 observations) but `first_seen` ticked forward again to `2026-08-19` despite no content change — further confirming this field resets on every run regardless of whether the underlying content actually changed, not just when it does. `BC-0130`'s `anomaly_ids` also unchanged from Day 3 (`[ANOM-0324, ANOM-0396, ANOM-0432]`), no new observations added. No status change from Day 3's SUPPORTED. Logged as reconciliation only, not a new save — the ID-instability finding is already captured in SAVE-001 and this is confirmatory, not new information.

Separately, today's new material event (BC-0039, see CLV-003) independently reproduces the same `ANOM-####` churn pattern a third time (history shows 5 different anomaly-ID sets across 4 events for one stable `candidate_id`) — noted here as corroboration, not double-counted as a second save.

### CLV-003
- Timestamp / as-of: 2026-08-19T23:06:21Z (BCA report timestamp)
- Candidate / workflow: BC-0039 (Business Candidate Analyst, Mode A) — lifecycle escalation WATCH → VALIDATING, recorded in `business-candidate-analyst/reports/material-events-2026-08-19.json`, reason "reached VALIDATING: distinct_sources>=2 (have 2); economic_consequence EVIDENCED"
- Exact claim: BC-0039's grouped evidence — spanning (a) SaaS operators provisioning one dedicated compute machine per customer instead of shared infrastructure, (b) individual AI-coding-tool users manually tracking their own remaining usage quota, (c) an AI vendor's quota-reset schedule changing without warning mid-period, and (d) hard provisioning limits blocking new customer signups during growth spikes — describes one underlying business opportunity with `identifiable_buyer: api_consumer`.
- Claim type: INFERENCE
- Evidence tier: T1 (single source: BCA's own grouping/promotion output across 4 reassessment passes, 08-10→08-19; not independently corroborated that all four threads share a buyer or a fix)
- Sources: `business-candidate-analyst/data/candidates.json` (BC-0039 record: `anomaly_ids=[ANOM-0109, ANOM-0143, ANOM-0316, ANOM-0405, ANOM-0446]`, `history` showing 4 separate evidence-accretion events 2026-08-10→2026-08-19); `business-candidate-analyst/reports/business-candidates-2026-08-19.md` (BC-0039 full dimension text)
- What would falsify it: A reciprocal-repair check, same standard already applied to CLV-001 — does fixing (a) (e.g. shared multi-tenant infra instead of per-customer machines) repair (b), (c), or (d)? Manual pass finds it does not: (a) is an **operator-side infrastructure architecture decision**; (b) and (c) are an **individual end-user's visibility into their own AI-tool usage**; (d) is an **operator-side capacity/onboarding limit**. These read as at least two, plausibly three, distinct mechanisms crossing two different buyer personas (SaaS operator vs. individual API consumer) bundled under one `identifiable_buyer: api_consumer` label. If a closer read of the underlying observations shows (a) is actually describing the same buyer/mechanism as (b)–(d) (e.g., the SaaS operator *is* the same actor experiencing the quota problem, just wearing two hats), the claim survives; as read from the report text alone, it does not.
- Decision dependency: If falsified, BC-0039's VALIDATING promotion — like BC-0130's — overstates corroboration: what reads as one opportunity with `distinct_sources=2` and 6 accumulated observations across 4 reassessment passes may functionally be 2–3 separate, more thinly-evidenced opportunities bundled by BCA's keyword/bucket heuristic (same mechanism already identified in CLV-001, `business-candidate-analyst/README.md`'s own documented limitation). Matters to research prioritization the same way CLV-001 does: trusting a VALIDATING label at face value here would overstate how corroborated any single one of these sub-claims actually is.
- Reused from earlier claim?: NO (new candidate, new material event) — but the *pattern* (heterogeneous threads bundled by BCA's keyword/bucket merge into one escalated candidate) is the same failure mode already identified in CLV-001, now observed on a second, independent candidate. Not counted as claim reuse; counted as corroborating evidence for that pattern, tallied separately in the final report.
- Same claim or changed claim?: N/A — first mention
- Current status: OPEN

**Reconciliation note, 2026-08-22 (Day 7) — major update:** `BC-0039` escalated again today, VALIDATING → **PROMISING** — the first `PROMISING` candidate the pipeline has produced since this experiment began (every prior day's summary read `PROMISING: 0`). Reason given: `willingness_to_pay EVIDENCED; economic_consequence EVIDENCED; scalability EVIDENCED; pain_severity == SEVERE`. `evidence_diversity` grew from 2 to **3 distinct sources** (`fly-io`, `openai-devs`, and a new `home-assistant`), `independent_observation_count` from 6 to 8.
Reading the three newest cited observations directly (`constraint-archaeology-agents/data/observations.jsonl`, full records, not just the report's truncated paraphrase):
- `OBS-20260819-0077-0bf871` (fly-io): a SaaS **operator** hitting a 100-machine provisioning cap while scaling infrastructure, needing manual limit-increase requests during signup spikes.
- `OBS-20260822-0042-2ae15f` (openai-devs, today's newest): an individual ChatGPT **end user** unable to switch an existing conversation from "Work mode" back to "Chat mode" after Work-quota exhaustion, forced to lose context by starting a new chat.
- `OBS-20260812-0008-2cf7ed` (home-assistant, the new third source): a **smart-home/IoT integration user** whose camera-device API polling consumes platform quota, forced to trade off stale device state against quota consumption.
These three, plus the original fly.io/openai-devs threads already flagged in this claim's initial entry, span **at least three unrelated domains and buyer personas** — B2B SaaS infrastructure operator, individual AI-chat end user, and smart-home hobbyist/integration developer — bundled under one `identifiable_buyer: api_consumer` label and one `scalability: weak_signal_multi_platform` value. The reciprocal-repair test already applied above still holds and is now starker: fixing ChatGPT's Work/Chat mode-switching does nothing for a Home Assistant camera's API-polling quota, and neither does anything for Fly.io's per-account machine provisioning cap. `BC-0039`'s own `anomaly_ids` field also changed a sixth time today (`[ANOM-0106, ANOM-0219, ANOM-0310, ANOM-0401, ANOM-0443, ANOM-0545]`), further corroborating SAVE-001 without being separately counted.
Classification: **CONTRADICTS**, more strongly than the Day 4 entry. Current status updated: **FALSIFIED** (not merely WEAKENED) — three independent, directly-quoted, unrelated failure modes bundled as one "multi-source-corroborated" opportunity is no longer a borderline inference call.

**Reconciliation note, 2026-08-20 (Day 5):** `BC-0130` unchanged (`anomaly_ids` still `[ANOM-0324, ANOM-0396, ANOM-0432]`, same as Day 3/4) — no new development. `ANOM-0362` checked a fourth time: today it reads **"Exporting AI-generated images and videos from Gemini/Veo/Google Flow tools — visible four-point sparkle watermark"** — a fourth, completely unrelated canonical_pattern (Day 0: fly.io idle machines; Day 1: orphaned subagents; Day 3–4: fly.io idle machines again; Day 5: Gemini/Veo watermark export). This is the clearest single data point yet for SAVE-001's underlying claim, but it doesn't change SAVE-001's status (already confirmed Day 3) — logged here as reconciliation/corroboration only, not a new save.

### CLV-004
- Timestamp / as-of: 2026-08-20T23:08:38Z (BCA report timestamp)
- Candidate / workflow: BC-0117 (Business Candidate Analyst, Mode A) — lifecycle escalation WATCH → VALIDATING, recorded in `business-candidate-analyst/reports/material-events-2026-08-20.json`, reason "reached VALIDATING: distinct_sources>=2 (have 2); economic_consequence EVIDENCED"
- Exact claim: BC-0117's two grouped anomalies — ANOM-0297 (`discourse:fly-io`: "users cannot find billing information in a multi-tenant cloud platform UI, search in expected-but-wrong locations") and ANOM-0301 (`discourse:openai-devs`: "developer blocked from OpenAI plugin creation despite completed identity verification and a Business-plan upgrade") — describe the same underlying business opportunity.
- Claim type: INFERENCE going in; resolved to a direct factual check (see below)
- Evidence tier: T4 for the falsification itself (both anomalies' own `canonical_pattern` text read directly, no interpretation required)
- Sources: `business-candidate-analyst/data/candidates.json` (BC-0117, `anomaly_ids=[ANOM-0297, ANOM-0301]`); `constraint-archaeology-agents/data/anomalies.json` (`ANOM-0297`, `ANOM-0301` full canonical_pattern text, quoted above verbatim); `business-candidate-analyst/reports/business-candidates-2026-08-20.md` (BC-0117's rendered dimension text — notably, the rendered report's `underlying_job_or_problem` field shows only billing-navigation paraphrases and never surfaces the plugin-creation content at all, because the field truncates with "…" after ~4 of 5 observation snippets)
- What would falsify it: reading each grouped anomaly's own `canonical_pattern` directly — done, this run. ANOM-0297 is a UI-findability complaint (a settings page isn't where a user expects it). ANOM-0301 is an access/permissions complaint (a verified, paying developer is blocked from a feature by what reads as a bug or unresolved verification-state issue). These do not share a mechanism, a fix, or, on this text, even a plausible causal link — this is not a borderline reciprocal-repair judgment call like CLV-001/CLV-003, it is two unrelated problems from two different products (Fly.io vs. OpenAI's platform) sharing only the coincidence of both mentioning "billing"/"account"-adjacent vocabulary somewhere in their text.
- Decision dependency: BC-0117's VALIDATING promotion rests entirely on `distinct_sources>=2`, and both of its two sources are now shown to describe unrelated problems — one of the two "corroborating" data points does not corroborate anything about the other. This candidate's real evidentiary strength is 1 source (ANOM-0297 alone), which does not clear the promotion bar on its own. Left uncorrected, this VALIDATING label would misrepresent a single-source, still-`WATCH`-grade finding as multi-source-corroborated.
- Reused from earlier claim?: NO — third independent instance of the CLV-001/CLV-003 false-merge pattern, not a reuse.
- Same claim or changed claim?: N/A — first mention
- Current status: **FALSIFIED** (resolved same-day; the claim that these two anomalies share a mechanism does not survive reading their own canonical_pattern text)

**Honesty note on this entry:** BC-0117 was initially expected, on first read of the rendered report's paraphrased summary, to be a plausible *counter-example* — a genuinely well-merged candidate, useful as balance against CLV-001/CLV-003's pattern (the report text alone reads as one coherent billing-UX complaint repeated five times). It was only checking the two source anomalies' own text directly — the discipline this ledger schema forces, not something the rendered report's truncated summary prompts on its own — that overturned that expectation. Recorded here specifically because the adversarial instruction asked to prefer disconfirming evidence; this is a case where checking, rather than assuming, reversed my own prior read.

**SAVE-003** (2026-08-20, Day 5, from CLV-004): `BC-0117`'s VALIDATING promotion merges two anomalies whose own canonical-pattern text describes unrelated problems (a UI billing-page-findability complaint vs. a blocked-plugin-creation/verification bug) from two different products, sharing no mechanism. This is the clearest and most directly-falsifiable instance of the pattern found so far (SAVE-002 required a manual reciprocal-repair inference; this one is refuted by reading two one-line fields side by side) — and it is specifically **not visible in the rendered report**, whose `underlying_job_or_problem` field truncates before showing the plugin-creation content, so a reader relying on the report text alone (or an LLM summarizing that report text alone, without being told to open the underlying anomaly records) would have no way to see it. Counts as a save on the strict criteria: "two distinct claims silently merged," directly.

**SAVE-004** (2026-08-22, Day 7, from CLV-003 reconciliation) — **the most consequential finding of this experiment:** `BC-0039` reached **PROMISING** today, the first (and, as of this run, only) candidate the entire pipeline has promoted to its top lifecycle tier since this experiment began. Reading the three observations that carried it there directly, not the rendered report's truncated summary, shows they describe: (1) a SaaS operator's compute-provisioning cap during customer growth, (2) an individual ChatGPT user's inability to switch conversation modes after quota exhaustion, and (3) a Home Assistant user's camera-integration API-polling-vs-quota tradeoff — three unrelated domains and buyer personas (B2B infra operator, individual AI-chat consumer, smart-home hobbyist), bundled under one `identifiable_buyer` and treated as mutually corroborating `distinct_sources=3` evidence for one opportunity. None of the three problems shares a fix with either of the others. This directly matches the strict inclusion criteria's clearest case — "two distinct claims silently merged" — now at three, and at the pipeline's highest-confidence output, not a marginal WATCH candidate. **This is exactly the scenario the ledger's own stated validation question was written to test:** a materially wrong analytical promotion, at the point where it would most likely draw real attention, exposed only by tracing a *specific, previously-logged, persistently-tracked claim* (CLV-003, opened Day 4) through to a state change three days later — not by any one day's report read in isolation.

*(written at experiment end — see §0 for the stop condition)*
