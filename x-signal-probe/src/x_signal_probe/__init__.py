"""X Signal Probe - a bounded, read-only, probe-only experiment.

Answers exactly one pre-registered question (see ../README.md): does X
carry unique, early, economically significant observations that
Discovery Lab's existing public sources do not already find. This
package has zero import dependency on ca_agents or
business_candidate_analyst (enforced by tests/test_safety.py) and never
writes anywhere outside x-signal-probe/data and x-signal-probe/reports.
"""
