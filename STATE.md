# discovery-lab — Project State

```yaml
project: discovery-lab
status: ACTIVE / BOOTSTRAP
current_phase: MANDATE_DRAFTING
current_step: Mandate passed an independent adversarial review (merge gate verdict APPROVE WITH MINOR CHANGES); Principle 0 adopted, Recommendation Ledger and Evidence Coverage defined as interfaces only; Ecosystem Observatory still recommended, still not accepted
blocked_by: no human decision yet on which mandate variant (if any) governs discovery-lab; no independent reviewer role defined for its own investigation reports; purpose and scope remain unconfirmed until a variant is accepted
last_completed: Reality Inbox architecture FROZEN. Requester asked to create "ADR-0002 - Reality Inbox Architecture," but ADR-0002 was already taken (the memory/ export-bridge decision, ACCEPTED - IMPLEMENTED); registered this one as ADR-0003 instead, flagged explicitly rather than silently renumbering anything. ADR-0003 (docs/adr/ADR-0003-reality-inbox-architecture.md, ACCEPTED - FROZEN) locks in two properties as fixed architecture: (1) the human-facing interface is exactly one folder, reality-inbox/DROP HERE/; (2) processing state is tracked only through manifests, never folder location. Section 3 draws an explicit, enforceable line between what now requires a new ADR (adding a second human-facing folder, moving state tracking out of manifests, changing the manifest schema or who may process files, weakening file-handling rules) versus normal operation that doesn't (processing real files, adding manifest/INDEX entries, writing the still-missing large-file size policy, extending use to another repository with the same design). Added one-line "FROZEN, see ADR-0003" status markers to reality-inbox/README.md and PROCESSING-PROTOCOL.md so the freeze is discoverable at the point someone would edit the structure. Also caught and fixed a real staleness bug while updating the ADR index: docs/adr/README.md still showed ADR-0002 as DRAFT even though the ADR file itself had already been marked ACCEPTED - IMPLEMENTED in an earlier task - corrected, per the index's own rule that the ADR file is authoritative.
next_action: Petko to decide whether/when to perform the first REAL Reality Inbox drop - the pipeline is verified twice (memory/ and reality-inbox/) but has never processed real content; this freeze doesn't block that, normal operation is explicitly exempted. No large-file size policy has been written yet. Cross-repository use (project-memory and others) is named as intended but not wired up. Any future change to the Reality Inbox's single-folder/manifest-only design now needs a new ADR (ADR-0004+) per ADR-0003 section 3 - not an ad-hoc edit. No further Google Drive MCP calls are planned - that path stays closed per Infrastructure Sprint 01 section 9. Separately, still open: whenever Petko authorizes starting ADR-0001's migration, ADR-0001-migration-plan.md's four items are ready; item 2's two-axis schema migration is still NOT STARTED. Also still open: review of PROP-0001 (mandate), FOUNDING-CHARTER.md (6 Candidate Conflicts), PROP-0002 (14 findings), PROP-0003 (10 findings), AG-002's PILOT-RUN-0001 Recovery Queue (4 items), RUN-0001 (AG-001)'s first ORB Review, and DL-0001's unrun verification experiment
updated_at: 2026-07-24
```
