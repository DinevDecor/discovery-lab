# discovery-lab — Project State

```yaml
project: discovery-lab
status: ACTIVE / BOOTSTRAP
current_phase: MANDATE_DRAFTING
current_step: Mandate passed an independent adversarial review (merge gate verdict APPROVE WITH MINOR CHANGES); Principle 0 adopted, Recommendation Ledger and Evidence Coverage defined as interfaces only; Ecosystem Observatory still recommended, still not accepted
blocked_by: no human decision yet on which mandate variant (if any) governs discovery-lab; no independent reviewer role defined for its own investigation reports; purpose and scope remain unconfirmed until a variant is accepted
last_completed: ADR-0004 created (docs/adr/ADR-0004-local-drive-synced-reality-inbox.md, ACCEPTED - DESIGN COMPLETE, AWAITING LOCAL VERIFICATION), amending ADR-0003's freeze after the requester pointed out the Reality Inbox still optimized for the repository, not the user. Decision: the primary human-facing intake folder for local sessions (Claude Desktop, local Claude Code) becomes "G:\My Drive\Projects\discovery-lab\DROP HERE" - an ordinary local folder Google Drive for Desktop already syncs, requiring only a file copy and a spoken instruction, no Git/GitHub interaction. reality-inbox/DROP HERE/ (git-tracked) is kept, unedited, as the documented fallback for sessions without local filesystem access. Rigorously confirmed (mount table, env vars, rclone check - all empty/absent) that THIS session is one of those: it runs in a remote, ephemeral Linux container with zero filesystem connection to the user's machine - a different, unrelated limitation from the earlier Drive-MCP-connector approval-flow problem (section 9), explicitly disambiguated in the ADR so the two aren't conflated. Repository-side manifests, provenance, and PROCESSING-PROTOCOL.md kept as-is; added one new manifest field, intake_mode (local-drive-sync | repo-tracked-fallback), and updated steps 1/8 to cover copy-vs-move by mode. Amended ADR-0003 with a pointer to this change (frozen text preserved, not rewritten). Updated reality-inbox/README.md, PROCESSING-PROTOCOL.md, MEM-004's registry notes, and the ADR index (4 ADRs now) accordingly. Honestly marked as UNVERIFIED from this session - no fabricated local-mode test was performed or claimed.
next_action: The local-mode workflow (ADR-0004) needs its first real exercise from a session that actually has it: Claude Desktop or local Claude Code, with Google Drive for Desktop syncing "G:\My Drive\Projects\discovery-lab\DROP HERE". That first real run is the next concrete step - copy a file there, tell the agent to process it, and confirm the agent can read the local path, hash/manifest/copy it into reality-inbox/processed/, and push the result, all without the human touching Git. Until that happens, ADR-0004 stays "design complete, unverified," not "implemented." Separately, still open: whether/when to perform the first REAL Reality Inbox drop of any kind (both verification runs to date used synthetic content); no large-file size policy written yet; cross-repository use not wired up; any future structural change to the Reality Inbox now needs a new ADR (ADR-0005+) per ADR-0003 section 3. No further Google Drive MCP calls planned - closed per Infrastructure Sprint 01 section 9, unrelated to this task's finding. Also still open: whenever Petko authorizes starting ADR-0001's migration, ADR-0001-migration-plan.md's four items are ready; item 2's two-axis schema migration is still NOT STARTED. Also still open: review of PROP-0001 (mandate), FOUNDING-CHARTER.md (6 Candidate Conflicts), PROP-0002 (14 findings), PROP-0003 (10 findings), AG-002's PILOT-RUN-0001 Recovery Queue (4 items), RUN-0001 (AG-001)'s first ORB Review, and DL-0001's unrun verification experiment
updated_at: 2026-07-24
```
