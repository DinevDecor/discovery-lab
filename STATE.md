# discovery-lab — Project State

```yaml
project: discovery-lab
status: ACTIVE / BOOTSTRAP
current_phase: MANDATE_DRAFTING
current_step: Mandate passed an independent adversarial review (merge gate verdict APPROVE WITH MINOR CHANGES); Principle 0 adopted, Recommendation Ledger and Evidence Coverage defined as interfaces only; Ecosystem Observatory still recommended, still not accepted
blocked_by: no human decision yet on which mandate variant (if any) governs discovery-lab; no independent reviewer role defined for its own investigation reports; purpose and scope remain unconfirmed until a variant is accepted
last_completed: Attempted to verify ADR-0004 locally, as requested. Result: BLOCKED, correctly, at the first precondition. Re-confirmed fresh (not assumed carried over) that this is the identical remote container as before (same CLAUDE_CODE_CONTAINER_ID, CLAUDE_CODE_REMOTE_ENVIRONMENT_TYPE=cloud_default) with no mount, network filesystem, or env var connecting it to the user's machine or Google Drive. One important negative result recorded precisely so it's never mistaken for progress: `mkdir -p "/mnt/g/My Drive/Projects/discovery-lab/DROP HERE"` succeeded (exit 0) - but this only proves Linux will create an arbitrary local directory tree under any name, not that it reached Google Drive. The resulting look-alike folder was empty, fully disconnected, and was deleted immediately (rm -rf /mnt/g) rather than left behind as a misleading artifact. Since there was no real reachable folder, steps 3 onward of the requested cycle (confirm read/write, detect a real diary file, copy the original, manifest with intake_mode: LOCAL_DRIVE, process, commit/push) were correctly not attempted on fabricated data. ADR-0004's status was NOT changed to VERIFIED, per the requester's own instruction and this repository's standing discipline. Documented the full attempt, with exact evidence, in ADR-0004 section 6 (new) plus updated status lines in the ADR itself and the ADR index.
next_action: The local-mode workflow (ADR-0004) still needs its first real exercise, and it can only come from a session that is actually local: Claude Desktop, or the `claude` CLI run directly on Petko's machine, with Google Drive for Desktop actually syncing "G:\My Drive\Projects\discovery-lab\DROP HERE". No remote session (this one included) can perform that test - confirmed twice now. Separately, still open: whether/when to perform the first REAL Reality Inbox drop of any kind (both verification runs to date used synthetic content, and this attempt added no real content either); no large-file size policy written yet; cross-repository use not wired up; any future structural change to the Reality Inbox now needs a new ADR (ADR-0005+) per ADR-0003 section 3. No further Google Drive MCP calls planned - closed per Infrastructure Sprint 01 section 9, unrelated to this ADR. Also still open: whenever Petko authorizes starting ADR-0001's migration, ADR-0001-migration-plan.md's four items are ready; item 2's two-axis schema migration is still NOT STARTED. Also still open: review of PROP-0001 (mandate), FOUNDING-CHARTER.md (6 Candidate Conflicts), PROP-0002 (14 findings), PROP-0003 (10 findings), AG-002's PILOT-RUN-0001 Recovery Queue (4 items), RUN-0001 (AG-001)'s first ORB Review, and DL-0001's unrun verification experiment
updated_at: 2026-07-24
```
