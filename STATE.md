# discovery-lab — Project State

```yaml
project: discovery-lab
status: ACTIVE / OPERATIONAL
current_phase: PHASE_1_COMPLETE
current_step: Human Acceptance - EXEC-009: ACCEPTED. Petko confirmed the submitted PARTIAL recommendation is correct and made several standing decisions explicit: (1) the ecosystem adopts the two-family sensor model - Internal State Sensors and External Evidentiary Sensors - as a standing architectural reference; (2) no universal sensor framework code shall be implemented at this stage; (3) five items (safety enforcement, configuration loading, CLI/orchestration shape, evidence-and-citation discipline, reporting conventions) are shared architectural PATTERNS, explicitly not yet shared runtime modules - a precise, narrower framing than "PROMOTE" in 3-SHARED-COMPONENT-INVENTORY.md, which this acceptance supersedes for exactly these five items (pattern documentation stands; a shared importable module does not exist and is not authorized); (4) trust classification, deduplication, and persistent registries remain optional capabilities belonging primarily to the External Evidentiary Sensor family; (5) no unified signal schema or unified confidence vocabulary shall be introduced - reconfirms 3-SHARED-COMPONENT-INVENTORY.md's own "DO NOT PROMOTE" verdict on both, now as explicit human policy rather than only this session's own analysis; (6) the EXEC-009 architecture documents (docs/proposals/EXEC-009-universal-sensor-framework/) remain the reference for future sensor design; (7) code extraction is to be reconsidered only when a third real sensor is implemented and provides additional evidence, or a separate human decision explicitly authorizes extraction of the triple-evidenced mechanical components - restates 5-MIGRATION-PLAN.md's own two triggers as binding, not just proposed. No repository was modified to record this acceptance beyond STATE.md/CHANGELOG.md themselves - operational baseline (main) untouched, per Petko's own explicit instruction.

Prior, same day: EXEC-009 - Universal Sensor Framework v1.0 (docs/proposals/EXEC-009-universal-sensor-framework/). Architectural consolidation task, explicitly not implementation - no code was written or modified; observation-agent/ and reality-sensor/ remain exactly as merged. Compared both existing sensors across 18 dimensions (2-COMPARATIVE-MATRIX.md) and found they are not one shape wearing different domain logic but two genuinely different sensor families: Internal/Deterministic (Observation Agent - no trust question, no duplicate-event question, ephemeral per-run diffing, no persistent ID) and External/Evidentiary (Reality Sensor - trust classification and deduplication central, persistent RS-000N registry). Applied the task's own Anti-Abstraction Rule literally ("every shared component must have evidence from at least two independent implementations") against every candidate the task itself proposed: found the task's own draft target-architecture had already over-abstracted once - three of its six named layers (Trust Classification, Deduplication, Signal Registry) are evidenced by only ONE of the two current sensors, while the single most strongly-evidenced shared component in the whole review (Safety enforcement - independently reinvented three times now across observation-agent/headquarters/reality-sensor's own test_safety.py files) wasn't in the task's diagram at all. Revised the proposed architecture accordingly (1-FRAMEWORK-SPECIFICATION.md): Safety enforcement, JSON-config-loading pattern, and the CLI shim/orchestration-separation shape promoted as mandatory cross-cutting components (3 independent implementations each); Trust Classification and Deduplication demoted to optional, family-scoped layers (1 of 2 sensors each, but directionally needed by most named future sensors - Research/Business/Procurement/Regulations/Markets are all External/Evidentiary); the persistent Signal Registry pattern promoted conditionally (evidenced by 2 convergent implementations - reality-sensor's own RS-000N mirrors headquarters/recommendation.py's HQ-000N, though both are executive-layer/single-sensor evidence, not two independent sensors); a single unified Signal/Finding schema and Relevance gating explicitly NOT promoted (would add meaningless fields to Observation Agent, or lack a second implementation). Delivered all 7 required items: Framework Specification, Comparative Matrix, Shared Component Inventory, Adapter Contract (Core Finding fields + what's mandatory/optional/domain-owned), Migration Plan (4 additive stages, zero required change to either existing sensor, Observation Agent permanently stopping after the mechanical Stage 3 items is named as a success state not an incomplete migration), Risk Analysis (over-abstraction - including naming that this very task's own source material over-abstracted once; future incompatibility; performance - none material found; governance), and a Recommendation. Verdict: PARTIAL - not ACCEPT (nothing to fully accept into, since EXEC-009 itself excludes building the framework package or adding new sensors), not REJECT (3 components are genuinely triple-evidenced and migration is provably additive) - accept the architecture and two-family model as the ecosystem's standing design reference, defer building actual sensor-framework/ code until a real third sensor is authorized or a human decides the triple-evidenced components are worth extracting on their own merits. No repository other than the addition of this proposal was modified; operational baseline (main) untouched, per the task's own explicit instruction.

Prior, same day: Human Acceptance - EXEC-008: ACCEPTED. Petko confirmed the implementation satisfies the architectural intent, accepted the capture/processing separation as a permanent design principle, confirmed the reported limitations are appropriate and honestly documented, and authorized a narrow-scope merge (no unrelated files). Executed: built claude/exec-008-narrow-deploy off main, isolated to exactly the 37 reality-sensor/ files (git archive from claude/prop-0002-discovery-intake, no STATE.md/CHANGELOG.md - same isolation method as every prior narrow merge), verified via git status --short and 61/61 tests passing in isolation, opened PR #11, squash-merged to main as acf281f. Post-merge validation: 61/61 tests passing from a fresh worktree of the merged main; a real live run against the committed validation dataset from the merged code produced byte-identical signal counts, IDs, and confidence levels to the pre-merge run (8 signals, same RS-0001..RS-0008, same confidence per signal); git status --short confirmed clean on all 5 ecosystem repositories throughout. Reality Sensor 001 is now merged operational infrastructure - the ecosystem's authoritative main-branch state is: Observation Agent 001 (v0.2, scheduled) + Ecosystem Headquarters v1.0 (human-invoked) + Reality Intelligence Sensor 001 (v0.1, human/executor-invoked capture + deterministic processing). CERT-001's own Baseline Fingerprint (commit 12f82fd) predates this merge and has not been updated - whether Reality Sensor 001 becomes part of a future baseline is a separate, later certification decision, not made here.

Prior, same day: EXEC-008 - Reality Intelligence Sensor 001 (reality-sensor/). First initiative after Phase 1's completion (Phase 1 itself remains ACCEPTED, unchanged by this work - Petko has not yet named a formal "Phase 2"). Built the ecosystem's first external reality sensor: External Reality -> Reality Sensor 001 -> Signal Registry -> Observation Layer -> Headquarters -> Human Decision, never bypassing Headquarters. Phase 0 discovery inspected observation-agent/ and headquarters/ conventions first (CONTRACT.md template, test_safety.py's forbidden-pattern discipline, models.py/Confidence-enum style, config.json pattern, HQ-000N persistent-ID pattern in recommendation.py) and reused them throughout rather than inventing parallel architecture. The one load-bearing design decision: EXEC-008 requires both "continuously observe live external reality" and "3 identical repeated executions produce identical output" - genuinely in tension, since no software can make live network calls and also guarantee byte-identical repeated output. Resolved by splitting capture (external, point-in-time, executor-mediated via WebFetch/WebSearch against the fixed Source Registry, producing a raw-captures JSON file) from processing (100% pure-Python, 100% network-free, fully deterministic - enforced by a new tests/test_safety.py check scanning for any network-client reference, extending the same static-detector pattern observation-agent's and headquarters' own safety tests already established). Built src/reality_sensor/ (models, config, trust policy, relevance gate, dedup/clustering, idempotent RS-000N registry mirroring recommendation.py's HQ-000N reuse-by-key pattern, daily/weekly brief renderers, cli.py) plus config/source-registry.json (20 fixed sources across the 4 named domains, each trust-classified) and config/relevance-gate.json (keyword mapping to the 5 named projects, first-draft heuristic grounded in each project's own available README/PROJECT_STATE content, honestly flagged as such). 61 tests covering all 12 EXEC-008-required categories (source validation, malformed feeds, duplicate suppression, evidence enforcement, trust classification, fact-vs-interpretation structural separation, stable IDs, repeated identical runs, empty result handling, read-only verification, Headquarters compatibility, regression against self-generated reports), all passing. Performed a real, bounded external capture pass (14 WebSearch/WebFetch operations - 3 blocked with HTTP 403 by anthropic.com/openai.com/arxiv.org's own bot protection, honestly documented rather than hidden) producing 10 real, sourced, quoted raw captures across all 4 domains (Claude Opus 5's 1M context window confirmed via a genuine PRIMARY-trust direct fetch of code.claude.com's own changelog; the MCP spec's stateless rewrite; Gemini 3.6 Flash family; GitHub Copilot/Code-Quality billing changes; one LLM-agent-memory research paper with an explicit, honest freshness-uncertainty caveat since arXiv blocked direct verification), committed as validation-dataset/raw-captures-2026-07-11-to-2026-07-25.json. Validated: 3 repeated executions against this fixed dataset held signals/IDs/evidence completely stable (8 -> 0 new/8 updated -> 0 new/8 updated, times_seen incrementing 1->2->3 correctly, zero duplicate evidence, zero ID churn) both in accumulating and fresh-directory modes; 1 live run produced real, evidence-cited reports/signal-registry.json plus Daily/Weekly briefs, including 2 genuine duplicate-source clusters (Opus 5 and the MCP spec change each independently confirmed by 2 real sources) proving duplicate suppression on real, not synthetic, data. git status --short confirmed clean on all 5 ecosystem repositories throughout. Headquarters compatibility proven structurally (signal-registry.json is a flat, tolerant-JSON-readable list matching headquarters/collector.py's own reading style) but NOT wired into collector.py itself - EXEC-008 says not to redesign the existing ecosystem and does not list a Headquarters code change among its required deliverables, so this is named as a small, separate, not-yet-attempted follow-on. PASS verdict per EXEC-008's own 9 named criteria - see reality-sensor/docs/VALIDATION-REPORT.md and the Final Report delivered to Petko. Explicitly not merged to main and no repository other than the addition of reality-sensor/ itself was modified - EXEC-008's own "Do not merge without explicit human approval" instruction, same narrow-scope discipline as every prior tool build in this engagement.

Prior, same day: Human Acceptance - Discovery Lab Phase 1: ACCEPTED. Petko declared Phase 1 complete, naming what the ecosystem now possesses - an operational Observation Layer (Observation Agent 001), an operational Executive Layer (Ecosystem Headquarters v1.0), evidence-based validation (EXEC-005), regression discipline (58/58 + 111/111 = 169 tests), certification discipline (CERT-001), and a certified operational baseline. Explicitly acknowledged the deferred baseline-v1.0 git tag as an environment limitation, not a certification defect - does not affect Baseline v1.0's CERTIFIED status. Reaffirmed the authoritative baseline: commit 12f82fd, certification CERT-001, status CERTIFIED. This directly followed CERT-001's own verdict update (below): PARTIALLY CERTIFIED -> CERTIFIED, after Petko's "Human Decision - CERT-001" authorized a narrow deployment of EXEC-006's fix to main (PR #10, squash-merged as 12f82fd, isolated to observation-agent/ only - 15 files, excluding STATE.md/CHANGELOG.md which don't exist on main) and all 5 required post-deployment verifications passed: real Observation Agent run (37 observations), real Headquarters run (Overall Health 71%), 3x repeated-run stability (37 -> 37 -> 37, 0 new after the first run), unchanged CI-path behavior (Scanned 1, skipped 4, STATUS: PARTIAL as expected), and HQ-0001 reconfirmed as Headquarters' sole recommendation (score 6, unchanged). CERT-001 itself (docs/certifications/CERT-001-ecosystem-baseline-v1.0/) is the first Ecosystem Baseline Certification - a 14-section certified-baseline document plus a 22-entry evidence appendix citing exact commands, file paths, and commit hashes for every claim, distinguishing implemented (exists in code) from deployed (merged to main) from certified (independently evidence-verified) - a distinction Petko's Human Decision explicitly made a permanent certification principle. Attempted to create the baseline-v1.0 git tag per the Baseline Release instruction; blocked by an HTTP 403 from the local git proxy when pushing a tag ref (branch pushes and PR merges through the same proxy succeeded without issue; no create-tag/create-release capability exists in this session's GitHub tooling either) - reported rather than retried, per the proxy's own organization-policy-denial guidance. Petko accepted this as an environment limitation, to be resolved manually when a normal git environment is available; it does not affect Baseline v1.0's certification status.

Prior, same day: EXEC-006 - Narrow Maintenance: Self-Observation Defect Fix (observation-agent/). Per Petko's Human Acceptance - EXEC-005 (verdict PASS WITH A NAMED LIMITATION, accepted), Petko authorized exactly one narrow maintenance task addressing only the self-referential feedback-loop defect and its direct consequences - explicitly not a new agent, not a full health-model recalibration. Implemented all 7 required items. (1) Added excluded_paths: a new path-segment-aware, configurable exclusion mechanism in observation_agent/scanner.py's walk_files - repo-relative paths like "observation-agent/reports" are excluded exactly, plus everything nested under them, without matching a similarly-prefixed sibling like "reports-extra" and without hardcoding any single path in code; wired through config.py (AgentConfig.excluded_paths) and every check function's signature (broken_references, orphan_files, stale_state, status_history_consistency, registry_check) plus cli.py's call site. (2) Both config.json and config.ci.json now declare excluded_paths: ["observation-agent/reports", "headquarters/reports"] - explicit and configurable, not hardcoded for one path only, proven equivalent between local and CI configs by a new test. (3) Reworded observation-agent/README.md's two prose examples so "[text]" and "(path)" are no longer written adjacently - verified directly against the actual regex that both observation-agent/README.md and headquarters/README.md are now CLEAN. Caught and fixed one self-inflicted recurrence of the exact same bug while documenting this very fix: three new Limitations-section sentences describing the defect itself initially reused the same [text]-immediately-followed-by-(path) phrasing, re-triggering the regex - reworded before commit, reverified CLEAN. (4) Added regression tests proving stability: tests/test_scanner.py (5 new tests covering excluded_paths mechanics), a new TestSelfReferentialFeedbackLoop class in tests/test_broken_references.py (3 tests reproducing the real defect and proving the fix resolves it), and a new tests/test_cli_stability.py (3 tests exercising the full CLI end-to-end via cli.main() against a fixture repo whose own reports/ output directory lives inside the scanned repo, exactly like the real layout - including a negative control proving these tests exercise a real fix, not an already-impossible scenario). Full suite grew from 45 to 58 tests, all passing. (5) Verified local/CI equivalence directly with a new TestExcludedPathsEquivalence class in tests/test_ci_activation.py (2 tests), not just asserted. (6) Confirmed via 3 repeated real Observation Agent runs against the live 5-repository ecosystem: 39 -> 39 -> 39 total observations, 0 new on the second and third runs (previously this compounded 2->10->58 before the fix); confirmed via a real Headquarters run that HQ-0001 remains the sole "Most Important Recommendation" (score 6, unchanged). (7) health.py's formulas were left untouched - Overall Health/metric calculations remain exactly as EXEC-004/EXEC-005 left them, per Petko's explicit instruction not to recalibrate the full health model in this change. Named, not fixed (explicitly out of this task's narrow scope): STATE.md, CHANGELOG.md, and INV-0004 itself still contain their own pre-existing [text]-immediately-followed-by-(path)-shaped prose examples describing this very defect - real, stable, non-compounding matches against ordinary repository content, not a recurrence of the reports/-directory feedback loop, left for a future, separately-scoped task. Verified via git status --short that all 5 repositories (including discovery-lab itself) remained unmodified beyond the intended source/test/doc changes; reverted tool-run artifacts to their committed state after validation (once catching and correcting an over-broad cleanup command that briefly deleted two already-committed report files, restored via git checkout before anything was lost).
blocked_by: Nothing blocks anything - EXEC-009 is ACCEPTED and closed as a standing architectural reference. Building sensor-framework/ runtime code is not blocked, it is explicitly not-yet-authorized by Petko's own policy: reconsider only when a third real sensor is implemented and provides additional evidence, or a separate human decision explicitly authorizes extraction of the 3 triple-evidenced mechanical components (safety enforcement, configuration loading, CLI/orchestration shape - documented as shared patterns now, not shared runtime modules). Trust classification, deduplication, and persistent registries remain optional External-Evidentiary-family capabilities, not to be generalized further without new evidence; no unified signal schema or confidence vocabulary is to be introduced, full stop, not merely deferred. Explicitly out of Reality Sensor 001's own scope, named as follow-on work, not attempted here: wiring headquarters/collector.py to actually read signal-registry.json (structurally proven compatible, not integrated); automating the capture step itself (currently executor-invoked only, per EXEC-008's own "no autonomous browsing" constraint); adding code.claude.com/docs/en/changelog and other sources found useful during validation to the fixed config/source-registry.json; a deeper review of config/relevance-gate.json's first-draft keyword mapping by someone with real knowledge of KOD/Trust Engine/Generative Discovery Engine/Dinev Assistant's actual priorities. Everything already open before EXEC-008 remains open and unchanged by it: a documented operator convention; scheduling Headquarters itself; broader health-model recalibration; rewording the [text]-immediately-followed-by-(path)-shaped prose examples in STATE.md/CHANGELOG.md/INV-0004; a cross-repository dependency-graph parser; the other 4 Drift categories EXEC-004 named; Recommendation Evaluation staying INSUFFICIENT_EVIDENCE until real decisions are recorded; registry.py's correction staying off main; cross-repository Observation Agent coverage staying discovery-lab-only; AG-002/AG-003 run-count findings; HQ-0001 itself remaining an open advisory recommendation; baseline-v1.0's git tag remaining deferred to manual creation; no independent reviewer role; the 6 PROPOSED Recommendation Ledger entries; G2's Unified Control Plane Specification's own adoption decision; discovery-lab's own absence from project-memory/PROJECT_REGISTRY.md; whether either tool is formalized into a full Employee Role.
last_completed: Human Acceptance - EXEC-009 (ACCEPTED). See current_step above for the full summary - the two-family sensor model adopted as standing architectural reference; the 5 shared items reclassified precisely as patterns, not runtime modules; Trust Classification/Deduplication/persistent registries confirmed optional and External-Evidentiary-scoped; unified schema/confidence vocabulary explicitly ruled out; docs/proposals/EXEC-009-universal-sensor-framework/ confirmed as the standing reference; both code-extraction triggers reconfirmed as binding policy. Operational baseline (main) untouched throughout.

Prior, same day: EXEC-005 - Ecosystem Operational Validation. Ran 3 full local Observation Agent + Headquarters pairs plus 1 real GitHub Actions scheduled Observation Agent run, no code changed in either tool (evidence-gathering only). Found and precisely documented one real defect: a self-referential feedback loop in repeated local runs (both tools' reports/ live inside discovery-lab, one of the 5 scanned repos; each run's own output becomes the next run's new input), root-caused to two literal examples of `[text]` immediately followed by `(path)` syntax in observation-agent/README.md's own prose that broken_references can't distinguish from real links - measured 2->10->58 new false positives and 35->45->103 "Human Decisions Required" items across 3 unchanged-input runs, confirmed absent from the CI/scheduled path (no memory between ephemeral runs). Verified recommendation SELECTION is robust to this noise - HQ-0001 stayed the sole, stable recommendation (same score 6) across all 3 runs, times_proposed incrementing correctly (2->3->4), no duplicate ID ever created - because the Attention Engine reads the Ledger and Headquarters' own Drift/Opportunity findings, not Observation Agent's raw observations directly. The noise did degrade a downstream display metric (Overall Health 71%->70%->67%) without ever changing which recommendation was selected - documented as Issue 2, a consequence of Issue 1. Assessed EXEC-005's 4 Success Criteria: 3 clean PASS (no-manual-intervention combined operation, explainable recommendations, read-only), 1 PARTIAL (output stability - the recommendation was stable, Overall Health was not, for a precisely identified reason) - overall determination PASS WITH A NAMED LIMITATION. Named 4 improvement opportunities, none implemented per the task's own explicit instruction at the time (2 of the 4 subsequently implemented by EXEC-006, above). Delivered as docs/investigations/INV-0004-ecosystem-operational-validation.md; all raw validation-run report/log/snapshot files inspected for evidence then removed/reverted to their pre-validation committed state. PROP-0001 remains RATIFIED (Variant B); G2's Unified Control Plane Specification v0.1 still separately DRAFT - Candidate for Adoption.

Prior, same day: EXEC-004 Deployment (Human Decision) - Petko accepted EXEC-004 for review and approved deployment: narrow-merge Headquarters onto main (PR #9, squash-merged as 8726ac1, exactly 32 files - headquarters/ minus reports/ - same isolation method as the Observation Agent's own EXEC-003 narrow merge, 111 tests passing in isolation, no dependency-chain blocker hit); do not implement HQ-0001 (advisory only); validate after merging. Validated via an isolated git worktree of the merged main commit (so the shared working directory's own branch state didn't interfere) - code from main, real ecosystem data via config.json's absolute paths: runs successfully, real Observation Agent consumption confirmed ("Scanned: project-memory, kod, discovery-lab, generative-discovery-engine, trust-engine"), exactly one recommendation (HQ-0001), full test suite and test_safety.py both pass from the merged code, git status clean on all 5 repos after the run. Wrote the requested HQ-0001 supporting analysis (advisory only, not implemented): the two "duplicate" ADR-0001 files are not a careless collision - the ADR's own closing section explicitly names its migration-plan companion file as deliberately deferred, not competing; presented three options (merge/supersede/retain-both-with-convention) and a recommendation (supersede/rename), left the actual decision to Petko. Delivered the first live main-generated report to the user as a file.

Prior, same day: EXEC-004 Additional Execution Directive - Inconsistency Classification, generic extensibility, and explicit INSUFFICIENT EVIDENCE reporting added to Headquarters. Petko's directive approved the earlier background survey as Headquarters' factual foundation and set three working rules: adapt to the ecosystem rather than reshaping it; record and classify inconsistencies rather than silently fixing them; never modify a repository automatically (all already true, now made explicit). Built inconsistency.py: Category constants (Implementation/Documentation/Governance/Data Quality/Unknown Issue), a fixed documented mapping from each Drift check's own category to one of the five, and a classify()/classify_all() pair that only ever processes Drift findings (never Opportunities, which are positive suggestions, not problems) and always populates all four required fields (affected artifact, observed evidence, operational impact, recommended future action) plus a category rationale. Added unparseable_state_files as a sixth Drift check specifically so Data Quality Issue has real coverage rather than staying a theoretical bucket - flags a configured state file that exists on disk but yields fewer than 2 parseable fields. Implementation Issue deliberately stays unpopulated in v1.0, documented as reserved for a future check rather than filled with a fabricated example. Wired an "Inconsistencies" section into the Executive Brief and changed the no-recommendation message to lead with the literal phrase INSUFFICIENT EVIDENCE, per the directive's Design Philosophy restating Observation Agent observes / Headquarters interprets and forbidding invented assumptions. Addressed the Robustness Requirement by replacing two hard-coded per-repo lookups with bounded, shallow, filename-pattern discovery: registry_consolidation used to hard-code exactly which registry files exist in kod and discovery-lab, now discover_registry_files finds any "*registry*.md" file via a depth-limited, excluded-dir-aware walk; shared_safety_pattern used to hard-code the exact paths to observation-agent's and headquarters' own test_safety.py files, now discover_safety_scanners matches any "<tool>/tests/test_safety.py" without naming either tool; proposals_without_state_reference used to hard-code discovery-lab as the only possible owner of docs/proposals/, now it reads the owning repository from config.proposals_dir dynamically. Both new discovery functions stay narrower than Observation Agent's own deep content scanning - filename-pattern matching within a depth bound, never reading file content - preserving the "Headquarters does not duplicate Observation Agent's repository scanning" boundary while still satisfying "new repositories, agents, registries and documents should be discoverable with minimal Headquarters changes." Added tests proving genuine extensibility rather than backward-compatible re-matching - fixtures use repo/file names appearing nowhere in the source (a-brand-new-repo-name, WIDGET_REGISTRY.md, brand-new-tool-a/tests/test_safety.py). Test suite grew from 90 to 111, all passing (10 for the taxonomy, 3 for the new Data Quality check, 5 proving generic discovery, 2 for the Inconsistencies brief section, plus fixes to existing Drift tests for the new config parameter). Reran the tool for real against the live 5-repository ecosystem: 15 inconsistencies classified this run (3 Governance Issue - the duplicate ADR-0001, and two PROJECT_REGISTRY.md gaps for discovery-lab and generative-discovery-engine; 12 Unknown - unreferenced docs/proposals/ subdirectories; 0 Data Quality/Documentation/Implementation this run, none fabricated to fill empty categories). HQ-0001 correctly recurred with its existing ID rather than being reassigned a new one (times_proposed incremented to 2 in recommendation-log.json), demonstrating the traceability system's own cross-run behavior on real, not fixture, data. Confirmed via git status --short that all 4 other repositories and discovery-lab itself remained unmodified beyond Headquarters' own source/tests/reports. Updated README.md with the classification taxonomy table, the Extensibility section (what's now generic vs. what still genuinely requires a code change - a new shape of artifact, not a new instance of a known shape), and the INSUFFICIENT EVIDENCE reporting discipline.

Prior, same day: EXEC-004 (initial build) - Ecosystem Headquarters v1.0 (headquarters/). Implements EXEC-004's own architecture closely (Collector, Health Engine, Portfolio Engine, Drift Detector, Opportunity Detector, Prioritizer/Attention Engine, Recommendation Engine + traceability, History Manager, Executive Brief Generator - Recommendation Engine and traceability share one module, recommendation.py, since they share one persistent log). Built a tolerant parsing layer (parsing.py) handling the three genuinely different real STATE.md shapes found across the five repos (fenced yaml, fenced plain-text, unfenced Key : Value under headings) without forcing a single schema, plus a fenced-block parser reused for the Recommendation Ledger's own multi-line summary fields. Health Engine computes 8 metrics, each with its formula stated in its own docstring and echoed into the rendered brief: State Visibility, Observation Cleanliness (read directly from the Observation Agent's own latest report, not recomputed), Decision Currency, Governance Integrity, Overall Health (mean of the other four, excluding any unavailable sub-score rather than zeroing it), Observation Coverage, Recommendation Backlog, Decision Backlog. Portfolio Engine builds one entry per the same fixed 5-repo scope Observation Agent already uses (PROP-0001's no-dynamic-discovery rule), each with Purpose/Current State/Trend/Dependencies/Last Meaningful Progress/Risk Level/Confidence/Health Score, honestly INSUFFICIENT_EVIDENCE where a field cannot be supported (Dependencies in every entry, v1.0's stated limitation). Drift Detector implements 5 of the 10 named categories (duplicate ADR IDs, stale ADRs, registry gaps, decision backlog, possibly-abandoned proposals) - the other 5 explicitly out of v1.0 scope, named as future evolution, not silently skipped. Opportunity Detector implements 3 mechanical, evidence-cited, always-DRAFT heuristics (shared safety-scanner duplication between observation-agent and headquarters themselves - a real, self-referential finding; multi-registry-with-no-index in kod and discovery-lab; oversized discovery-lab CHANGELOG.md). Attention Engine (Prioritizer) assembles a candidate pool from Ledger PROPOSED entries plus this run's Drift and Opportunity findings, scores each with a fully shown, documented rubric (+3/+1/+0 by confidence tier, +2 for small-to-finish, +2 for unblocking the Ledger's own acceptance_rate, +1-per-repo breadth capped at 2, -1 for being a new-work opportunity - the guiding principle operationalized directly in the weights, not just stated), and selects exactly one - never a list, enforced by construction in brief.py, which only ever renders one recommendation under "Most Important Recommendation." Recommendation Engine assigns persistent HQ-000N identifiers (reused across runs for a recurring candidate, confirmed by a real second-run test); Recommendation Evaluation reads a human-maintained recommendation-decisions.json and honestly reports INSUFFICIENT_EVIDENCE until one exists with real entries, rather than fabricating a score. History Manager compares each run's headline numbers to the immediately previous run (first run correctly reports "N/A," not a fake trend). Built a 90-test suite (parsing, health, drift, opportunity, portfolio, prioritizer, recommendation, history, brief, collector, plus a real end-to-end CLI integration test against a fabricated fixture ecosystem proving no fixture file was modified) including a safety scanner reusing observation-agent's own forbidden-pattern detector, extended with a Headquarters-specific check that no source file references any HTTP/network client - all 90 passing. Ran the tool for real against the live 5-repository ecosystem: first real run genuinely selected a concrete, previously-known-but-never-fixed defect (discovery-lab's duplicate ADR-0001 - two files, ADR-0001-human-authority-gates.md and ADR-0001-migration-plan.md, both claiming the same ID) as HQ-0001, the single highest-value next action, over the Recommendation Ledger's 6 PROPOSED entries (none yet overdue under the 3-day threshold used) and three DRAFT opportunities (correctly scored lowest, per the guiding principle). Overall Health computed as 71% with its formula shown. Confirmed via git status --short that all 4 other repositories and discovery-lab itself were unmodified by the run - the only new content is headquarters/ itself (source, tests, docs, and its own reports/ output). Constraints honored: no repository modification of any kind, no self-approval anywhere (every Opportunity stays DRAFT, every Recommendation stays "proposed" until a human edits recommendation-decisions.json by hand), Observation Agent's own repository-scanning was not duplicated. Verdict: PASS on every one of EXEC-004's ten named Success Criteria - runs successfully, consumes Observation Agent outputs, produces ecosystem health, evaluates portfolio, selects exactly one highest-priority recommendation, explains its reasoning, generates an Executive Brief, remains read-only (enforced), passes tests, completed one verified operational execution.

Prior, same day: EXEC-003 - Activate Observation Agent 001 (.github/workflows/observation-agent.yml). Resolved both named Before-Activation factual items first: AG-002/AG-003 confirmed unchanged at INSUFFICIENT_EVIDENCE (no new human evidence arrived); kod/Infrastructure/python/registry.py corrected in INV-0003 and RECOMMENDATION-LEDGER.md from "0-byte empty" to "33-byte non-empty stub" (distinct from the two files that really are empty). Selected GitHub Actions as the mechanism (repository-native, genuinely read-only-capable): daily cron (06:00 UTC) plus workflow_dispatch, contents: read only at both workflow and job level, persist-credentials: false on checkout. Investigated cross-repo access for the other 4 configured repositories and confirmed via the GitHub API that all 5 are private repos in the same org - the workflow's default token cannot read the other 4 without a credential this session has no path to provision (a scoped PAT secret, or per-repo Actions-access grants, both administrative actions on repositories outside reach here); rather than escalate to a write-capable credential, built config.ci.json so discovery-lab is scanned fully via its own checkout and the other 4 SKIP safely, using the agent's existing, already-tested failure-safe behavior - documented as remaining human actions in README's new CI Limitations section, not silently worked around. Added ci_summary.py (CI-only, outside the agent's own source) to publish a SUCCESS/PARTIAL/FAILURE run summary to the Actions Job Summary. Added tests/test_ci_activation.py (15 tests: config loading, ci_summary's status classification, and static checks - reusing test_safety.py's own detector - that the workflow declares read-only permissions and contains no write/commit/push/PR action); full suite now 45/45 passing. Proved the exact CI config correct via a local dry-run matching the workflow's own working-directory (caught and fixed one real bug in the process: discovery-lab's path needed to be ".." not ".", given the workflow runs from inside observation-agent/). Updated README (schedule, manual-run procedure, permissions, artifact location, failure behavior, how a human transfers an accepted finding into the Recommendation Ledger, how to disable the schedule immediately) and CONTRACT.md (v0.2). Committed and pushed all of the above to claude/prop-0002-discovery-intake. Attempted the task's own required validation step - trigger one real workflow run through the exact mechanism the schedule will use - and hit a concrete, empirically-confirmed external blocker: GitHub Actions does not recognize, schedule, or allow workflow_dispatch for a workflow file that exists only on a non-default branch (actions_list returned 0 workflows; a direct dispatch attempt returned 404). Did not merge this branch to main to force activation - that would be a hard-to-reverse action on shared state, immediately activating a real recurring cron in live infrastructure, and this task's own instructions describe the deliverable in terms of a commit and branch, not a merge; matches this entire engagement's Human Final Authority boundary (PROP-0001's explicit "ACCEPT" before any status change; G2 remaining "Candidate for Adoption" pending its own ratification). Verdict at that point: BLOCKED - on a one-line human merge decision (this branch, or just the workflow file, into main), not a technical rebuild.

UPDATE, same day - Human Decision resolved the blocker: Petko approved a narrow-scope merge (observation-agent/ + .github/workflows/observation-agent.yml only, explicitly not the full task branch, and explicitly not docs/investigations/INV-0003/RECOMMENDATION-LEDGER.md, which don't exist on main and would have dragged in unrelated engagement history). Built the narrow changeset on a fresh branch off main (claude/activate-observation-agent, 28 files, verified via git status --short to contain nothing else), ran the full 45-test suite on it in isolation (proving no hidden dependency on excluded files), opened PR #8, and merged it (squash) to main as commit 428e18f. Confirmed via the GitHub API: the workflow is now recognized (state: active, 0 workflows before, 1 after). Triggered a real workflow_dispatch on main - run completed in 10 seconds, conclusion: success. Verified from the actual runner logs: GITHUB_TOKEN Permissions "Contents: read, Metadata: read"; persist-credentials: false (auth header explicitly unset post-checkout); console output "Scanned 1 repositories, skipped 4. 2 total observations (2 new, 0 repeated, 0 resolved)."; artifact observation-agent-report-1 (3 files, 2311 bytes) uploaded, retained 90 days; no push/commit/write command anywhere in the logs. Confirmed via git status --short that discovery-lab and all 4 other observed repositories remained unmodified. Reported the registry.py placement conflict to Petko rather than deciding it unilaterally, per his own stop-and-list-dependencies instruction. Petko's Human Acceptance message then explicitly ACCEPTED the implementation and activation, and confirmed all three deferrals (registry.py placement, cross-repo coverage, AG-002/AG-003 findings) exactly as executed - none required further tool or process changes. Verdict: EXEC-003 ACCEPTED. Observation Agent 001 is now a permanent operational read-only service; its outputs are declared valid evidence inputs for the next ecosystem layer.

Prior, same day: EXEC-002 - Build Observation Agent 001 (docs/proposals/AGENT-001-observation-agent/'s implementation, under observation-agent/). The session's first real implementation task, not an architecture or proposal exercise. Built a working Python package (models, config, a read-only scanner, five check functions - broken_references, orphan_files, stale_state, status_history_consistency, registry_check - report rendering with run-over-run diffing, and a CLI entry point) plus a 30-test suite including a static safety-verification test (tests/test_safety.py) that scans the actual source text for forbidden write/delete/commit/push/merge/subprocess patterns and restricts writing-mode file opens to exactly the two modules that write this tool's own reports/ directory - not just documented as read-only but enforced. Ran the tool for real against all 5 configured repositories; the first run surfaced two previously-unknown STATUS.yaml/HISTORY.md mismatches inside discovery-lab itself (AG-001, AG-002) plus a third in AG-003, none found anywhere earlier in this session. Rather than trusting the tool's own first output, independently verified all three by hand and found: AG-001's finding was a genuine false positive in the check's own regex (a bug-fix HISTORY.md heading's prose incidentally contained "RUN-0001," miscounted as a second run); AG-002's and AG-003's findings were real undercounts (run identifiers not using a literal "RUN-N" primary subject, and one heading bundling three run IDs into one). Fixed the regex to require RUN-N as a heading's primary subject immediately after the date and dash (precisely fixes the false positive), and downgraded a count mismatch's confidence from MISMATCH to INSUFFICIENT_EVIDENCE (the check cannot mechanically distinguish a bookkeeping gap from an unrecognized heading convention) rather than chasing further regex complexity to match every possible naming scheme - consistent with how stale_state and registry_check already handle the same category of proxy question. Added regression tests reproducing both exact real-world scenarios; full 30-test suite passes. Reran the real example execution with the fix - AG-001 now correctly appears under Resolved Findings, AG-002/AG-003 now correctly report INSUFFICIENT_EVIDENCE with a human-review recommendation instead of a false MISMATCH claim - and replaced the earlier, known-defective report output with the corrected run. Wrote README.md (usage, safety guarantees, an explicit Limitations section naming every check's scope boundary) and a lightweight CONTRACT.md (deliberately not a full Employee Role document set, per EXEC-002's own constraint against new governance). Constraints honored: no additional governance, no new architecture, no new proposal, no new review cycle, reused AGENT-001's exact schema and PROP-0001's existing vocabulary/rules including the no-single-aggregate-score rule (enforced by its own test), zero writes to any observed repository. Verdict: FULLY OPERATIONAL - runs end-to-end against all 5 real repositories, produces a real evidence-cited report with correctly-calibrated confidence, covered by a passing test suite with an enforced safety guarantee, and the one real defect its own first run surfaced about itself was fixed and verified rather than left as "good enough." No external blocker found or claimed.

Prior, same day: STRATEGIC-001 - Close the Evidence-and-Accountability Loop (self-directed initiative, under docs/proposals/STRATEGIC-001-close-evidence-loop/). Reviewed ARCH-001 through AGENT-001 without repeating any of it; identified the ecosystem's largest unmanaged risk as PROP-0001's own stated invalidation conditions for Variant B being currently uncheckable (no record existed of what Discovery Lab has found or what became of it); chose to close that gap as the single highest-value next step, ahead of the AGENT-001 Observation Pilot, a trigger/scheduling mechanism, an Independent Reviewer role, and other registry improvements, each explicitly named and deferred with reasons. Filed DL-001's real findings (previously chat-only) as docs/investigations/INV-0003-ecosystem-health-review-v0.1.md - the durable artifact type PROP-0001's own Variant B design specifies, provenance stated explicitly. Built docs/investigations/RECOMMENDATION-LEDGER.md for the first time - PROP-0001 fully specified this interface in its own text and explicitly deferred creating it until a real recommendation existed; populated with 6 real entries (REC-0001-REC-0006: project-memory x2, KOD x3, trust-engine x1), all status PROPOSED, none inferred accepted/rejected. Caught and fixed one real instance of this session's own recurring cross-repository relative-path bug during pre-commit verification (both new files initially cited project-memory's notes/2026-07-19-... as if same-repo-reachable; fixed to explicit prose, same pattern as the Reality Stress Test fix). Applied one small, separate, self-authorized correction: AG-001's STATUS.yaml (runs_completed 0->1, last_run null->2026-07-24) corrected to match its own HISTORY.md, per GOVERNANCE.md's own bug-fix-tier rule (no version bump, no lifecycle re-entry, no new human decision required). Constraints honored: no new governance model (both new documents use PROP-0001's own already-specified formats verbatim); Human Final Authority not bypassed (every recommendation stays PROPOSED; zero writes to project-memory, kod, trust-engine, or generative-discovery-engine, confirmed directly). Verdict: EXECUTED. What remains, named honestly: delivering the 6 recommendations to their destination repositories' own maintainers is outside Discovery Lab's authority and is the one human step this initiative could not itself complete.

Prior, same day: AGENT-001 (Observation Agent proposal, Idea-stage only) - recognized AG-001-repository-observer already exists and proposed a new, smaller Role built on its proven discipline plus a Recommended Action step PROP-0001's own info-flow map already specifies but never built; found the AG-001 STATUS.yaml mismatch (later fixed in STRATEGIC-001); specified the full Observation Loop, a 7-field Observation Model, Event Source classification (reconfirming DL-002's no-trigger-mechanism finding per-category), and one recommended human-invoked pilot. Verdict: READY FOR OBSERVATION PILOT (human-invoked only). DL-001 (Ecosystem Health Review v0.1) executed PROP-0001's own first specified experiment for real across all 5 fixed repositories, found real MISMATCHes including one that revised ARCH-002's own G1 finding (kod has real if minimal implemented code, traced to an APPROVED SPRINT-024); verdict PASS (process-completion sense only). DL-002 (Autonomy Readiness Assessment v1.0) mapped Human Authority Boundaries (including a newly-named live tension: this session has performed git commit/push as an AI executor throughout, sitting in unexamined tension with project-memory's INV-4 taken literally); found no ratified trigger/scheduling mechanism exists anywhere; verdict NOT READY FOR FIRST AUTONOMY PILOT, narrowly. DL-001/DL-002 were both delivered as chat reports only at the time, no files written, per their own explicit "do not modify repositories" constraints - DL-001's findings were subsequently filed durably by STRATEGIC-001, above.
next_action: PRIMARY, newest: EXEC-009 is closed - docs/proposals/EXEC-009-universal-sensor-framework/ is the standing reference for all future sensor design, no further action pending on it. Code extraction stays not-yet-authorized until one of Petko's own two named triggers fires: a third real sensor gets built and provides additional evidence, or a separate human decision explicitly authorizes extracting the 3 triple-evidenced mechanical patterns (safety enforcement, configuration loading, CLI/orchestration shape) into actual runtime code - neither is requested now. SEPARATELY: Reality Sensor 001 remains merged (main, acf281f) with its own follow-on work named but not attempted, available whenever a human wants to authorize it: wiring headquarters/collector.py to actually read signal-registry.json; automating the capture step itself (a separate, later, human-authorized decision about how a fetch capability would be safety-reviewed and scoped); adding code.claude.com/docs/en/changelog and other sources found useful during validation to config/source-registry.json; a human review of config/relevance-gate.json's first-draft keyword mapping by someone with real knowledge of each project's actual priorities; deciding whether CERT-001's Baseline Fingerprint should be updated to include Reality Sensor 001 (a separate, later certification decision). Phase 1 remains complete and ACCEPTED, unaffected by this new work; Phase 2's formal scope has not yet been named by Petko - nothing below is a Phase 1 blocker, all of it is either a standing advisory item awaiting a human decision or explicitly deferred work available whenever authorized. SEPARATELY, still open: HQ-0001 (discovery-lab's duplicate ADR-0001 - two files, ADR-0001-human-authority-gates.md and ADR-0001-migration-plan.md, both claiming the same ID) remains the open, evidence-backed advisory recommendation awaiting Petko's decision - accept a fix (merge/supersede/retain-both-with-convention, per the EXEC-004-deployment supporting analysis), or defer - Headquarters was explicitly instructed not to implement it itself; CERT-001 reconfirmed it is still the sole recommendation on the certified baseline. SEPARATELY: baseline-v1.0's git tag is deferred to manual creation when a normal git environment is available (acknowledged as an environment limitation, not a certification defect). Two of EXEC-005's 4 named improvement opportunities remain explicitly deferred, available whenever a human wants to authorize that work: a documented operator convention; scheduling Headquarters itself. Also still named, out of every prior task's narrow scope: STATE.md, CHANGELOG.md, and INV-0004 itself still contain their own pre-existing `[text]`-immediately-followed-by-`(path)`-shaped prose examples describing the EXEC-005/EXEC-006 defect - real, stable matches against ordinary content, not a recurrence of the reports/ feedback loop, left for a future task. Whether/how to activate Headquarters on a schedule (mirroring EXEC-003) is a distinct, later, human-authorized decision, not yet requested. Three items remain explicitly DEFERRED from EXEC-003's Human Acceptance (not pending Discovery Lab action, should not be re-raised without a new human request): registry.py's correction placement on main (stays on claude/prop-0002-discovery-intake until INV-0003/RECOMMENDATION-LEDGER.md are independently reviewed and accepted); cross-repository coverage beyond discovery-lab in the scheduled CI run; and AG-002/AG-003's INSUFFICIENT_EVIDENCE run-count findings (no further parser work requested). Separately, still open: the Recommendation Ledger's 6 PROPOSED entries (docs/investigations/RECOMMENDATION-LEDGER.md) are still ready to be taken to project-memory's and kod's own maintainers for a real accept/reject decision - a human step, per Principle 0, not one Discovery Lab can perform itself. Once any entry receives a real decision, updating its status (PROPOSED -> ACCEPTED/REJECTED/PENDING_NO_RESPONSE) is itself a small, self-authorized bookkeeping act. Whether to formalize the Observation Agent into a full Employee Role (e.g. extending or superseding AG-001) is a separate, later, human-authorized decision, deliberately not taken here. G2's Unified Control Plane Specification v0.1 still awaits its own, separate adoption decision. The wider no-trigger-mechanism gap for actions other than the Observation Agent's own reporting (DL-002) remains the deepest blocker to any future truly autonomous (not human-invoked) capability in this ecosystem - resolving it would need its own dedicated proposal. The described DLOS-CORE-0001 freeze remains a live option for Petko to decide, scoped narrowly to P3 (or the P1/P2/P3/P4 validation-discipline cluster), per META-001's own finding - unchanged by ARCH-001 through this task, none of which revisited it. Separately, still open from the RI-0002 meta-theory work: the sample-selection critique (AG-002's GRIF-format selection may pre-select for rigor-performing content) remains unresolved. Still open from the freeze: the real, unresolved maturity re-scan loophole (releases/1.0/VALIDATION-HISTORY.md item 16); who conducts the first genuinely independent ORB Review of any frozen Role, or of ARCH-001/002/003/EXEC-001/G2/PROP-0001-package/DL-001/DL-002/AGENT-001/STRATEGIC-001 themselves. Also still open: review of FOUNDING-CHARTER.md (6 Candidate Conflicts), PROP-0002 (14 findings), PROP-0003 (10 findings), AG-002's PILOT-RUN-0001 Recovery Queue (4 items), and DL-0001's unrun verification experiment.
updated_at: 2026-07-25
```
